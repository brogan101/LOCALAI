# PHASE 0 REPORT — Baseline Audit + Dead Weight Removal

Completed: 2026-04-18

---

## Files Changed

| File | Change |
|---|---|
| `BASELINE_REPORT.md` | Created — full audit (file tree, endpoint matrix, export map, dep audit, hardcoded strings) |
| `artifacts/localai-control-center/package.json` | Removed 26 unused production dependencies |
| `artifacts/localai-control-center/vite.config.ts` | Removed `radix` entry from `manualChunks` |
| `pnpm-lock.yaml` | Regenerated by `pnpm install` (2 packages removed) |

---

## Steps Completed

### 0.1 — BASELINE_REPORT.md
Written to disk. Covers:
- Full file tree grouped by layer (routes, lib, config, pages)
- Endpoint-to-UI matrix: ~130 endpoints, 100 WIRED, 25 WRAPPER-ONLY, 1 ORPHANED
- Export-to-import map: 1 dead export found (`runBatch` in file-execution-agent.ts)
- Dependency audit: 26 unused frontend prod deps, `drizzle-orm` deferred to Phase 5
- Hardcoded strings: 12× `127.0.0.1:11434`, 9× direct model-roles.json reads, model names scattered — all deferred to Phase 1+

### 0.2 — Remove Unused Frontend Dependencies
Removed all 26 zero-import production packages from `package.json`:
- All 27 `@radix-ui/*` packages
- `framer-motion`, `sonner`, `recharts`, `cmdk`, `vaul`, `embla-carousel-react`
- `react-day-picker`, `react-hook-form`, `@hookform/resolvers`
- `react-icons`, `react-resizable-panels`, `tailwind-merge`, `tw-animate-css`
- `next-themes`, `input-otp`, `zod`, `date-fns`, `clsx`, `class-variance-authority`

`drizzle-orm` retained in api-server/package.json — deferred to Phase 5 per instructions.

### 0.3 — vite.config.ts
Removed `radix` key from `build.rollupOptions.output.manualChunks`. Only `react` chunk remains.

### 0.4 — WorldGUI Submodule
Submodule directory exists and is initialized with upstream files. No TypeScript source references it. README already documents it as reference-only (install separately). No action needed.

### 0.5 — pnpm install
Ran cleanly. 2 packages removed from lockfile. No `-` override entries in `pnpm-workspace.yaml` (Rule 5 compliant).

---

## Verification Gate Results

| Check | Result |
|---|---|
| `BASELINE_REPORT.md` exists and complete | ✓ |
| `pnpm install` → clean | ✓ (2 packages pruned) |
| `pnpm -r typecheck` → 0 errors | ✓ |
| `pnpm --filter localai-control-center build` → success | ✓ (430 kB bundle, built in 2.46s) |

---

## Issues Deferred

| Issue | Deferred To |
|---|---|
| `drizzle-orm` unused in api-server | Phase 5 |
| `config/models.config.ts` missing — Rule 4 violation | Phase 1 |
| 12× hardcoded `127.0.0.1:11434` literals | Phase 1 |
| 9× direct `model-roles.json` reads (no shared accessor) | Phase 1 |
| Model name string literals scattered across 7 backend files | Phase 1 |
| `runBatch` dead export in file-execution-agent.ts | Low priority / Phase 1 |
| POST /observability/thoughts — no frontend caller | Acceptable (future use) |
