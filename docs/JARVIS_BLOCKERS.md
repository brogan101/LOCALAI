# Jarvis Blockers

Any blocker must be logged here before an agent finishes a phase. Do not hide failures in chat output only.

## Active blockers

Phase 23 note (2026-05-02): no blocking Final Coverage Audit implementation blockers were found. Audit confirmed all 30+ requirements in JARVIS_REQUIREMENTS_TRACEABILITY.md are either implemented (foundation or complete) or captured in B-009 (future durable executor follow-through). Small doc gaps fixed: Phase 22 UI note added to JARVIS_UI_STYLE_GUARD.md, Phase 22 context index entry added, Phase 22 Maintainer mode note added to JARVIS_EXPERT_MODES.md, JARVIS_REQUIREMENTS_TRACEABILITY.md final-coverage-audit row updated to implemented. One new blocker added: B-012 (Project Foreman cross-system workflow surface is required by JARVIS_EXPERT_MODES.md but has no dedicated phase yet). All 6 Jarvis docs updated. Full verification suite passed: node scripts/jarvis/verify-build-kit.mjs, pnpm -r typecheck, pnpm test, pnpm --filter localai-control-center build.

Phase 22 note (2026-05-01): no blocking Local AI Transition implementation blockers were found. Phase 22 adds a local-first, approval-gated self-build workflow via `lib/local-builder.ts`. Hard limits cloudEscalationEnabled=false, selfModificationAllowed=false, and requireApprovalForEdits=true are TypeScript literal types — structurally impossible to claim cloud escalation, self-modification, or unapproved execution. Shell metacharacter phaseIds and self-modification targetFiles trigger hardBlocked=true on proposals; hard-blocked proposals never execute. All four local evals run with usedNetwork=false (verified by test assertions) and contain no secrets (regex-verified). Four context packs in docs/context-packs/ are non-empty (sizeBytes > 100 verified). Profile patches cannot enable cloud escalation or self-modification (verified by saving a profile and re-checking status). The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 21 note (2026-05-01): no blocking Packaging/Install/Backup/Restore/DR implementation blockers were found. Phase 21 adds a local-first recovery source of truth in `lib/packaging-recovery.ts`, metadata-only backup manifests, restore validation/dry-run, approval-gated restore proposals, safe helper scripts, README install/DR guidance, and an Operations Recovery tab. Backup manifests cover SQLite DB, app settings, integration config refs, prompt/context docs, generated workflows/templates, and model role metadata without embedding raw secrets, private backup contents, or model blobs. Restore dry-run returns `liveDataModified:false`; restore proposals require a current-state backup manifest and approval; approved restore still returns `not_configured` because destructive restore execution is intentionally disabled in Phase 21. Optional installer/external backup providers report `not_configured` rather than fake success. No service install, startup task, firewall change, PATH edit, external backup provider, destructive restore, delete/reset/reinstall action, network call, Docker, Python, cloud API, or external service is required by default. The remaining active item is B-009 (future service-specific approved durable executor follow-through, now also covering any future destructive restore executor).

Phase 20 note (2026-05-01): no blocking UI/UX Integration implementation blockers were found. Phase 20 is a polish-only phase: grouped sidebar navigation, Dashboard status strip, and shared StatusBadges components. All 9 nav group section labels render without touching any route or safety system. RuntimeModeCard, PendingApprovalsCard, and UpdaterStatusCard use existing query keys (`["runtime-mode"]`, `["approvals-dash"]`, `["updater-dash"]`) and existing API methods (`api.runtime.get()`, `api.approvals.list()`, `api.updater.selfMaintainer()`). No fake-ready states are introduced; UnavailableCard shows honest not_configured/degraded/blocked status. No route, page, safety guard, approval gate, audit event, or permission tier was removed or weakened. The 24-assertion `ui-integration.test.tsx` SSR suite uses renderToStaticMarkup + QueryClient.setQueryData — no DOM or browser required. `pnpm -r typecheck`, `pnpm test` (including all prior API/UI suites), and `pnpm --filter localai-control-center build` all pass. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 19 note (2026-05-01): no blocking Robotics Lab implementation blockers were found. Phase 19 adds a local simulator-first Robotics Lab source of truth in `lib/robotics-lab.ts`. All 9 optional providers (ROS 2, MoveIt 2, Nav2, Gazebo, Ignition Gazebo, depth camera, ROSBridge, Foxglove, Docker ROS) default `not_configured` with `executionEnabled=false`, `hardwareEnabled=false`, `simulationEnabled=false`, and do not fake connectivity. `execute_motion` and `navigate` are permanently blocked by `PHASE_19_BLOCKED_ACTIONS`; no approval unblocks them. Gripper, arm, firmware, relay, and serial/USB actions are `manual_only`; API never executes them. `physicalHardwarePresent: false`, `simulationOnly: true`, `hardwareExecutionBlocked: true`, and `executed: false` are TypeScript literal types — structurally impossible to claim hardware presence or execution. Robot profile names are redacted to remove passwords, tokens, API keys, and camera frame references before storage. Audit metadata stores IDs/counts/status/flags only; no raw sensor data, camera frames, map data, IP addresses, or location/presence data is logged. No ROS node, topic, service, simulator process, serial/USB write, firmware flash, actuator command, or external service call executed during tests. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 18 note (2026-05-01): no blocking Automotive Master Tech implementation blockers were found. Phase 18 adds a local automotive/vehicle diagnostics source of truth that reuses Digital Twin vehicle refs, Evidence Vault/RAG evidence refs, approval requests, audit/mission replay metadata, optional integration status, and existing UI patterns instead of creating duplicate vehicle/diagnostics systems. Optional python-OBD, pyOBD reference, ELM327, ELM327-emulator, SavvyCAN, OVMS, ACES log import, CAN interface, and external vehicle-data providers report not_configured/disabled and do not fake connectivity. Vehicle facts distinguish confirmed/user_provided/inferred/stale/unknown/not_configured; likely causes remain hypotheses, not confirmed faults, until test evidence exists. The Foxbody build profile preserves the required 1988 Mustang GT hatchback/LQ4/4L80E/ACES/BTR/FAST/JEGS/Z28/On3/wiring facts. OBD scan returns not_configured; clear-code/CAN/actuator/bidirectional actions are approval-gated; denied approvals do not execute; approved actions still return not_configured without configured providers; ECU write, tune change, and firmware flash are manual_only. Audit/replay records store IDs/counts/hashes/status/action flags, not VIN/title/private diagnostic logs, credentials, tokens, or secrets. No real OBD/CAN/ECU/scanner/serial/Bluetooth/hardware/cloud/external provider action executed. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 17B note (2026-05-01): no blocking Inventory/project-to-reality implementation blockers were found. Phase 17B adds a local inventory/project-to-reality source of truth that reuses Digital Twin source refs, Maker Studio project/material concepts, approval requests, durable jobs, audit events, mission replay metadata, and existing UI patterns instead of creating duplicate project/maker systems. Optional InvenTree, Snipe-IT, HomeBox, Spoolman, and PartKeepr providers report not_configured and do not fake sync/connectivity. Availability, quantity, and suitability distinguish confirmed/proposed/inferred/stale/missing/unknown; unknown inventory is not guessed as available. Purchase, reorder, vendor quote, QR/label print, NFC write, and delete actions are proposal-only/approval-required and return `executed:false`; denied approvals do not execute; approved external provider actions return not_configured until a later approved executor exists. QR/NFC label plans generate local data only and do not print/write. Audit/replay records store IDs/status/count/provider/action flags, not vendor tokens, purchase secrets, private inventory contents, project/customer data, addresses, or private files. No external inventory API, vendor API, purchase/order, label printer, scanner, NFC writer, sync, device, cloud, or network action executed. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 17A note (2026-04-30): no blocking Digital Twin implementation blockers were found. Phase 17A adds a local relationship graph that links existing Evidence Vault/RAG, HomeLab, Home SOC, Maker Studio, Edge Nodes, Home Autopilot, vehicle/tool/project refs, approval/audit, and UI patterns instead of creating duplicate inventory systems. Digital Twin entities and relationships are local SQLite metadata only. Unknown entity state, owner, location, configuration, provider data, and relationships remain unknown/proposed/not_configured and are not guessed. AI-created or inferred relationships require provenance; confirmed relationships require high confidence; relationship deletion marks records deleted; entity archive blocks active relationships unless forced, and forced archive marks links stale. Physical actions delegate to existing safety policies and return executed=false. No discovery, scan, sync, pairing, device/network/home/shop/vehicle API call, physical action, cloud call, or external service executed. Audit/thought metadata stores IDs/status/counts/metadata keys only and does not log private maps, endpoints, location/presence data, vehicle records, project data, credentials, tokens, or secrets. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 16 note (2026-04-30): no blocking Home SOC implementation blockers were found. Phase 16 extends the existing HomeLab source of truth instead of creating a duplicate SOC system. Optional Wazuh, Zeek, Suricata, OPNsense IDS/IPS, Pi-hole, AdGuard Home, LibreNMS, Zabbix, Netdata, Uptime Kuma, and osquery providers report not_configured/degraded/disabled without fake success. Alert/report records are local SQLite metadata only and distinguish confirmed facts, inferred possibilities, unknowns, and proposed next actions. Packet capture/sniffing is blocked by default. Dangerous remediation actions require approval; denied approvals do not execute; approved remediation still returns not_configured until a real security/network provider executor is intentionally configured in a later phase. Firewall/DNS/VLAN-style remediation gates can link to the existing Phase 15B config proposal pipeline but do not apply changes. Audit/thought records store IDs/status/count/provider/action flags, not raw logs, packet contents, DNS query contents, private/public IP maps, VPN data, credentials, tokens, or secrets. No real security/network API call, scan, packet capture, IDS/IPS change, firewall/DNS/DHCP/VLAN change, or device quarantine executed. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 15B note (2026-04-30): no blocking HomeLab config pipeline implementation blockers were found. Config generation extends the existing HomeLab source of truth instead of creating a second system. Draft proposal records are local SQLite metadata only and default to draft/proposal/dry_run. Optional NetBox, Nautobot, Proxmox, OPNsense, UniFi, Ansible, OpenTofu/Terraform, Docker Compose, and Batfish providers report not_configured/degraded/disabled without fake success. Validation distinguishes static, simulated, unavailable-provider, and real-provider states; unavailable Batfish/provider validation returns not_configured. Apply is blocked before validation, blocked without backup/rollback metadata, approval-gated for mutable HomeLab paths, and still not_configured after approval unless a real provider is intentionally configured in a later executor phase. Denied approvals do not execute. Audit/thought records store IDs/status/count/provider flags, not raw config contents, private IP maps, credentials, tokens, or secrets. No real firewall/VLAN/DNS/DHCP/routing/Proxmox/Docker/Ansible/OpenTofu/OPNsense/UniFi/NetBox/Nautobot/Batfish API call or config apply executed. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 15A note (2026-04-30): no blocking HomeLab Architect implementation blockers were found. `applied: false` is a TypeScript literal type on `HomelabBlueprint` — structurally impossible to claim config was applied. All 8 optional providers (NetBox, Nautobot, Proxmox, OPNsense, UniFi, Ansible, OpenTofu, Batfish) default `not_configured` and report reasons/descriptions without faking connectivity. VLAN IDs are validated 1–4094; subnet prefixes are validated as IPv4 CIDR with sane octet and prefix-length checks. Thought log metadata for devices never includes `managementIpRef`, serial numbers, or credentials; subnet metadata never includes raw IP prefixes or gateway addresses. `generateBlueprint()` always returns `applied: false`; there is no code path that sets `applied: true`. No firewall, VLAN, DNS, DHCP, routing, or device configuration changes were applied or queued. No real NetBox/Nautobot/network device API was called. HomeLab page at `/homelab` is fully wired (lazy-loaded, Network nav icon, route). The remaining active item is B-009 (future approved durable executor follow-through).

Phase 14B note (2026-04-30): no blocking Home Autopilot implementation blockers were found. `camera_frame_capture`, `camera_snapshot`, `stream_access`, and all recording-change actions are permanently blocked by the `BLOCKED_HOME_ACTIONS` Set and cannot be unblocked by any profile setting or approval. Compressor start/stop and main shop power are permanently `manual_only` — physical danger tier, requires human at the device. Garage door open/close/toggle, lock/unlock, heater, relay, alarm, and vacuum clean-zone require `approval_required` and return `requiresApproval: true`; denied/missing approvals do not execute. Unknown HA entities (not in the entity allowlist) are blocked regardless of action. Unknown MQTT topics (not in topic allowlist) are blocked for publish. All three evaluation functions (`evaluateHaAction`, `evaluateMqttPublish`, `evaluateDeviceAction`) return `executed: false` as a TypeScript literal type — execution is structurally impossible. Thought log metadata never includes endpoint URLs, HA auth tokens, MQTT credentials, camera frames, or private IPs. All providers default `not_configured`; no Home Assistant API, MQTT broker, robot vacuum, camera NVR, or shop device API was called during tests. The remaining active item is B-009 (future approved durable executor follow-through).

Phase 14A note (2026-04-30): no blocking Edge Node Architecture implementation blockers were found. The gaming PC `alwaysOn` field is permanently `false` and cannot be changed by any profile input or approval. Camera frame capture is permanently `blocked`. Shop relay/power control is permanently `manual_only`. No services were installed to remote nodes, no home/shop/device APIs were called, and no real nodes were required for tests. Health checks are read-only HTTP probes that skip gaming PC nodes entirely. All edge actions return `executed: false` — no remote mutation occurs in Phase 14A. The remaining active item is B-009 (future approved durable executor follow-through).

Blocker repair note (2026-04-29): B-003, B-004, B-005, B-006, B-008, B-010, and B-011 were rechecked and resolved or hardened in this pass. PowerShell 7 and Windows PowerShell both respond on the host, browser diagnostics now use the bundled Node v24.14.0 fallback when the API process is on Node v20.20.2, `pnpm audit --prod` completes with no known vulnerabilities after the `diff` upgrade, NVIDIA NVML telemetry returns live RTX 5070 VRAM data, generated FastAPI scaffold tests use a concrete health-title test name, WorldGUI text/key routes queue approval with redacted hash/length metadata instead of executing directly, and LOCALAI-owned docs/integration hints now prefer `127.0.0.1`.

Phase 12A note (2026-04-30): no blocking Business Module Foundation implementation blockers were found. Business modules are draft-first and local-first. Chatwoot, Twenty CRM, Cal.com/Cal.diy, Postiz, email, and SMS adapters default to disabled/not_configured and never fake success. Lead-generation external outreach drafts create tier4_external_communication approvals, but Phase 12A still does not execute sends, syncs, bookings, posts, CRM writes, installs, starts, purchases, or scheduled external actions. Stealth bots, spam blasting, anti-bot evasion, and external sends without approval are hard-blocked. Adapter profiles store secret references only and status responses do not expose secrets. Audit/mission replay records use hashes, lengths, IDs, and statuses instead of raw lead/customer/message contents. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 12B note (2026-04-30): no blocking IT Support Copilot implementation blockers were found. Generated scripts default to review/dry-run mode and include purpose, admin requirement, reads, changes, backup/restore plan, logging path, dry-run/WhatIf behavior, exit codes, and proof steps. Windows Event Log, AD/GPO, Fortinet/FortiAnalyzer, Ivanti, Exchange/Microsoft 365, and real script execution integrations report not_configured/disabled without fake success. Script execution proposals require approval; denied approvals do not execute; approved proposals still return not_configured because the real script executor is intentionally disabled for Phase 12B. Shared command sanitizer now blocks destructive IT admin patterns such as AD removal, GPO modification, firewall changes, and uninstall commands. Audit records store hashes/status/validation metadata, not raw ticket text or secrets. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 13A note (2026-04-30): no blocking Maker Studio Foundation implementation blockers were found. Maker Studio is a local foundation/control layer backed by SQLite maker projects/materials/CAD artifact metadata plus existing approval requests and audit events. Optional FreeCAD, CadQuery/build123d, KiCad, slicer, printer, CNC/laser, Obico, Spoolman, and InvenTree integrations report not_configured/disabled and cannot execute. Physical/fabrication actions are proposal-only, approval-required, or manual-only at machine; Phase 13A does not slice, print, send G-code, start CNC/laser, flash firmware, control hardware, install tools, start services, or contact external providers. Audit records store IDs/status/tier/count metadata, not private project contents or secrets. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 13B note (2026-04-30): no blocking FreeCAD/CAD-as-Code/KiCad adapter implementation blockers were found. Phase 13B extends the existing Maker Studio source of truth with provider status/proposal surfaces for FreeCAD MCP, CadQuery, build123d, OpenSCAD-style scripts, gNucleus Text-to-CAD MCP, BuildCAD AI, and KiCad MCP/CLI. Local CAD/electronics providers report not_configured until explicitly configured; cloud text-to-CAD providers report disabled/not_configured and never receive local design data by default. CAD/KiCad proposals are metadata-only review/dry-run records in `maker_cad_artifacts`; no macro, CAD/PCB executable, cloud call, render/export, slicer, printer, CNC, laser, firmware, G-code, manufacturing, or hardware action executes. Audit records store IDs/status/provider/action flags, not private project contents or secrets. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 13C note (2026-04-30): no blocking 3D Printer/Slicer/Spoolman/Obico workflow implementation blockers were found. Phase 13C extends the existing Maker Studio source of truth with provider status/proposal surfaces for OrcaSlicer, PrusaSlicer/SuperSlicer, OctoPrint, Moonraker/Klipper, Mainsail/Fluidd, FDM Monster, Spoolman, and Obico. Slicers, printers, material inventory, and monitoring integrations report not_configured/disabled without fake success. Slicing proposals are metadata-only dry-run/config-validation records in `maker_cad_artifacts`; print queue/start/heater/motor actions are approval-required or blocked; denied approvals do not execute; missing/unknown material can block queue proposals; Obico monitoring reports not_configured/degraded and never fakes active monitoring. No slicer executable, G-code generation, file upload, printer API call, print queue/start, heater/motor command, monitoring API call, cloud provider, or hardware action executes. Audit records store IDs/status/provider/action flags, not printer tokens, private URLs, project files, or secrets. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 13D note (2026-04-30): no blocking CNC/Laser/CAM/Electronics Bench Safety Console implementation blockers were found. Phase 13D extends the existing Maker Studio source of truth with provider status/proposal surfaces for FreeCAD Path/CAM, CNCjs, LinuxCNC, FluidNC, bCNC, LightBurn-style laser workflow, KiCad electronics bench, and serial/USB shop devices. CAM/CNC/laser/electronics providers report not_configured/disabled without fake success. Setup sheets are metadata-only proposal records in `maker_cad_artifacts`; CAM/toolpath actions are approval-required proposals; G-code send, machine motion, spindle, laser, firmware, relay/power, serial/USB, and dangerous electronics bench actions are manual-only or blocked with no execution. No live toolpath generation, G-code send, axis/machine motion, spindle/laser fire, firmware flashing, relay/power command, serial/USB write, machine API call, cloud provider, or hardware action executes. Audit records store IDs/status/provider/action flags, not serial IDs, device tokens, private project files, proprietary designs, or secrets. The remaining active item is B-009 (future service-specific approved durable executor follow-through).

Phase 11 note (2026-04-30): no blocking Voice/Screen/Meeting implementation blockers were found. `captureMode=disabled` by default — nothing records until the user explicitly enables it. Always-on capture is permanently hard-blocked and cannot be enabled by any profile or approval. Capture indicator is always visible when any mode is active. Cloud STT/TTS remain disabled (Phase 02 policy) — only local faster-whisper sidecar and Piper are used by default; both fail soft with 503/not_configured when unavailable. Wake word detection returns `not_configured` because no wake word engine is installed. Screenpipe-style screen context is disabled/not_configured — no always-on screen capture occurs. Meeting session follow-up sends require tier4_external_communication approval; no email/calendar/message/task is sent automatically. Raw audio and full transcripts are never stored server-side. The remaining active item is B-009 (future approved durable executor follow-through).

Phase 10 note (2026-04-30): no blocking Coding Agent Runtime implementation blockers were found. Self-modification, direct main apply, and destructive commands are permanently hard-blocked and cannot be enabled by any profile setting or approval. All file edits require tier3_file_modification approval with a diff and rollback payload before execution. Optional coding adapters (Aider, OpenHands, Roo, Cline, Continue) return `not_configured` without an installed/running adapter. The execute-refactor route returns 202+approvalRequired when no approvalId is present, and 403 when an invalid or mismatched approvalId is supplied. No adapter was installed, no files were modified by the agent, and no external network calls were made during tests. The remaining active item is B-009 (future approved durable tool/integration/gateway executor follow-through).

Phase 09B note (2026-04-29): no blocking Desktop Automation implementation blockers were found. WorldGUI is optional and returns `not_configured` without an installed/running WorldGUI. Credential entry, keylogging, and sensitive-window screenshot capture tiers are permanently hard-blocked and cannot be enabled by profile or approval. Click/type/keys/macro/app-launch/app-close/destructive actions require approval. Banking, password manager, security, and system-admin apps are in the default excluded-app list and cannot be targeted. No WorldGUI was installed or started, no window was focused, no input was sent, and no external network calls were made during tests. The remaining active item is B-009 (future approved durable tool/integration/gateway executor follow-through).

Phase 09A note (2026-04-29): no blocking Browser Automation implementation blockers were found. Playwright MCP is optional and returns `not_configured` without a configured Playwright MCP installation. Credential entry, anti-bot evasion, and cookie capture tiers are permanently hard-blocked and cannot be enabled by profile or approval. Form submit/login/purchase/download actions are Tier4 approval-required. No browser was launched, no Playwright MCP was installed or started, no page was navigated, and no external network calls were made during tests. The remaining active item is B-009 (future approved durable tool/integration/gateway executor follow-through).

Phase 08B note (2026-04-29): no blocking Evidence Vault implementation blockers were found. Paperless-ngx is optional and returns `not_configured` without a configured base URL. Secret-classified evidence records are blocked from RAG ingestion before `rag.ingest()` is called. Reminder proposals are dry-run/proposal only with `calendarIntegrationStatus: "not_configured"`. No private document contents, VINs, vendor names, or secret values appear in audit/thought logs or test output. The remaining active item is B-009 (future approved durable tool/integration/gateway executor follow-through).

Phase 08A note (2026-04-29): no blocking Professional RAG implementation blockers were found. Optional MarkItDown, Docling, OCR, LanceDB, and Qdrant integrations are intentionally status/config surfaces only and return `not_configured` rather than fake success. The existing hnswlib/local vector path remains the default working path. The remaining active item is future approved durable tool/integration/gateway executor follow-through.

Phase 07C note (2026-04-29): no blocking OpenClaw/NemoClaw gateway safety-wrapper blockers were found. OpenClaw and NemoClaw/OpenShell are optional future gateways and default to disabled/not_configured/dry-run/proposal behavior. Gateway records are attached to the Phase 07A tool firewall and Phase 07B isolation concepts; unknown sources are blocked, quarantined/rejected skills cannot execute, external communication is approval-gated, install/update behavior is proposal-only, and secrets/env/private data are blocked by default. No OpenClaw/NemoClaw repo was cloned, no service was installed or started, no skill was installed, and no gateway action executed. Some older broad hardening items remain deferred below for later targeted phases.

| ID | Date | Phase | Severity | Blocker | Reason/evidence | Impact | Required next action | Owner/status |
|---|---|---|---|---|---|---|---|---|
| B-012 | 2026-05-02 | Phase 23 audit | Low | Expert mode 6 (Project Foreman) requires a cross-system workflow surface connecting idea → plan → files → inventory → fabrication → installation → maintenance, but no dedicated Project Foreman phase or UI exists. | JARVIS_EXPERT_MODES.md §6 describes Project Foreman as a required expert mode. Phase 23 audit confirmed the building blocks (Inventory at `/inventory`, Maker Studio at Studios, Digital Twin at `/digital-twin`, Evidence Vault at `/evidence`) exist but are not unified into a single Project Foreman entry-point workflow. | Planned features that depend on Project Foreman routing (cross-system project intake → inventory check → CAD/slicer → fabrication → install flow) will be unavailable until a future Project Foreman phase exists. | Add a future Project Foreman phase that creates a unified cross-system project intake form, status board, inventory check delegation, Maker/CAD/print handoff, and final documentation package. Reuse existing `/inventory`, `/studios/maker`, `/digital-twin`, and `/evidence` APIs without duplicating their source of truth. | Deferred - non-blocking |
| B-009 | 2026-04-25 | Phase 00 / Phase 07A-21 follow-through | Medium | Integration/tool/gateway/business/IT-support/maker/HomeLab/Home SOC/Digital Twin/inventory/automotive/recovery adapters still need service-specific approved durable execution follow-through. | Phase 07A removed direct `exec(...)` launches from integration install/start/update routes and routes them through `tool-registry.ts` firewall decisions with `executed: false`. Phase 07B kept Docker MCP Gateway status/profile/proposal-only and did not start containers or execute MCP tools. Phase 07C kept OpenClaw/NemoClaw gateway status/profile/skill/action paths proposal-only and did not clone/install/start gateways or execute skills/actions. Phase 12A keeps Chatwoot/Twenty/Cal/Postiz/email/SMS adapters disabled/not_configured or proposal-only and does not send, sync, post, book, install, start, or write CRM records even after an approval is created. Phase 12B keeps generated script execution approval-gated and returns not_configured even after approval because the real script executor is disabled. Phase 13A keeps Maker integrations not_configured/disabled and physical actions proposal-only, approval-required, or manual-only; no slicer, printer, CNC, laser, electronics, firmware, G-code, or hardware executor exists. Phase 13B keeps FreeCAD/CadQuery/build123d/OpenSCAD/KiCad providers not_configured/proposal-only and cloud text-to-CAD disabled/not_configured; no CAD/PCB/macros/render/export/cloud/manufacturing executor exists. Phase 13C keeps slicer/printer/Spoolman/Obico/FDM Monster providers not_configured/disabled/proposal-only; no slicer executable, printer API, file upload, G-code generation, queue/start, heater/motor, monitoring, or hardware executor exists. Phase 13D keeps FreeCAD Path/CAM, CNCjs, LinuxCNC, FluidNC, bCNC, LightBurn-style laser, KiCad electronics bench, and serial/USB shop devices not_configured/disabled/proposal-only/manual-only; no live toolpath, G-code send, machine motion, spindle/laser fire, firmware, relay/power, serial/USB, API, cloud, or hardware executor exists. Phase 15B keeps HomeLab config apply approval-gated and returns blocked/not_configured without configured providers, validation, approval, and backup/rollback metadata; no NetBox/Nautobot/Proxmox/OPNsense/UniFi/Ansible/OpenTofu/Docker Compose/Batfish executor exists. Phase 16 keeps Home SOC providers not_configured and remediation proposal/approval-only; no Wazuh/Zeek/Suricata/OPNsense IDS/Pi-hole/AdGuard/LibreNMS/Zabbix/Netdata/Uptime Kuma/osquery/firewall/router/security API executor, scan, packet capture, DNS/filter change, quarantine, VLAN isolation, or security-tool mutation exists. Phase 17A keeps Digital Twin as local graph/search/detail/safety metadata only and performs no discovery, sync, device/network/home/shop/vehicle API, cloud, or physical action. Phase 17B keeps InvenTree/Snipe-IT/HomeBox/Spoolman/PartKeepr, vendor APIs, purchases/reorders, label printing, NFC writing, scanners, and external inventory sync not_configured/proposal-only with `executed:false`. Phase 18 keeps python-OBD, ELM327, ELM327-emulator, SavvyCAN, OVMS, ACES log import, CAN interfaces, external vehicle-data providers, clear-code/CAN/actuator/bidirectional actions, ECU writes, tune changes, and firmware flashing not_configured, approval-gated, or manual_only with `executed:false`. Phase 21 keeps destructive restore not_configured even after approval until a future executor can verify manifest hash, current-state backup, approval payload, and rollback plan. Real adapter/executor follow-through is intentionally not implemented yet. | Integration, MCP, Docker MCP, OpenClaw, NemoClaw, OpenShell, messaging bridge, skill adapter, CRM/support/calendar/social/email/SMS, IT support script executor, maker/CAD/printer/CAM/CNC/laser/electronics integrations, HomeLab network/infrastructure providers, Home SOC/security providers, Digital Twin source adapters, inventory/vendor/label/NFC providers, automotive/OBD/CAN/ECU providers, recovery restore executors, and physical/fabrication/security/vehicle/workflow actions are safe-by-default but cannot yet perform approved durable execution after human approval. | In a later tool/integration/gateway/business/IT/maker/HomeLab/Home SOC/Digital Twin/inventory/automotive/recovery executor phase, add service-specific durable jobs that verify matching approval payloads, run exact reviewed adapters/scripts/config applies/remediations/syncs/purchases/label/NFC/OBD/CAN/log-import/restores in sandboxed/scoped mode, capture exit code/logs/provider diffs, preserve backup/rollback/diagnostic evidence, and redact private customer/lead/message/ticket/log/project/CAD/printer/machine/device/network/security/entity/inventory/vendor/purchase/VIN/vehicle diagnostic/recovery contents. | Deferred - non-blocking |

## Resolved blockers

| ID | Date resolved | Phase | Fix summary | Proof |
|---|---|---|---|---|
| B-011 | 2026-04-29 | Blocker repair | Standardized LOCALAI-owned README, integration, repair, studio, web-search, and WorldGUI local URLs/hints on `127.0.0.1`, while preserving `localhost` only in explicit trusted-loopback allowlists/parser logic. | Search over README/docs/API/UI/scripts shows no remaining LOCALAI setup/integration hint `localhost` hits outside this blockers file, historical ledger text, and legitimate allowlist/parser code. |
| B-010 | 2026-04-29 | Blocker repair | Routed `/worldgui/type` and `/worldgui/keys` through `approval_requests` with `tier4_external_communication` and `p4_approval_required`; responses return 202 and include only input length/hash metadata, with no direct text/key execution. | `pnpm --filter api-server run test:permission-routes` passed with 174 assertions and verifies raw desktop input is not echoed. |
| B-008 | 2026-04-29 | Blocker repair | Renamed the generated FastAPI template test from `test_placeholder` to `test_health_app_title_matches_project`. | API typecheck passed; generated template source no longer contains the scaffold-only test name. |
| B-006 | 2026-04-29 | Blocker repair | Current host NVML telemetry now works; no code fallback removal required. | `nvidia-smi --query-gpu=name,driver_version,memory.total,memory.free --format=csv,noheader` returned `NVIDIA GeForce RTX 5070, 596.21, 12227 MiB, 10443 MiB`. |
| B-005 | 2026-04-29 | Blocker repair | Re-ran dependency audit, found one low `diff@7.0.0` advisory, upgraded API dependency to patched `diff@8.0.3`, and updated lockfile. | `pnpm audit --prod` now reports `No known vulnerabilities found`; targeted tests and API typecheck passed. |
| B-004 | 2026-04-29 | Blocker repair | Added browser-tooling diagnostic fallback detection for `LOCALAI_BROWSER_NODE_PATH` or the Codex bundled Node runtime; current host bundled Node is v24.14.0, satisfying the required `>=22.22.0` browser runtime while the API process remains on Node v20.20.2. | `getBrowserToolingDiagnostic()` returns status `ok` with browser fallback `v24.14.0`; `runtime-diagnostics.test.ts` passed. |
| B-003 | 2026-04-29 | Blocker repair | Rechecked host PowerShell availability; tray sidecar already prefers `pwsh.exe`, and both PowerShell 7 and Windows PowerShell respond on this host. | `pwsh` returned `7.6.0`; `powershell.exe` returned `5.1.26100.8115`. |
| B-007 | 2026-04-25 | Phase 00.5 | Replaced root `clean` script with `node scripts/clean-workspace.mjs`, a bounded cross-platform cleanup that removes only repo build-output paths and refuses paths outside the repo root. | `pnpm -r typecheck` and `pnpm test` passed after package script update. |
| B-001 | 2026-04-25 | Phase 00.5 | API live startup and health check verified manually from the Windows host/browser context. Codex shell URL probing remained limited with `connect UNKNOWN` / service-provider socket errors, now classified as a Codex execution-context limitation. | User manually reached `http://127.0.0.1:3001/api/health`; API server stayed running. |
| B-002 | 2026-04-25 | Phase 00.5 | UI live startup verified manually from the Windows host/browser context. Codex shell URL probing remained limited with `connect UNKNOWN` / service-provider socket errors, now classified as a Codex execution-context limitation. | User manually reached `http://127.0.0.1:5173`; UI dev server stayed running. |
