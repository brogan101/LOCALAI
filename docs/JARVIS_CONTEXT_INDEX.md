# JARVIS_CONTEXT_INDEX

Purpose: compact repo map for Codex and future local AI agents. Keep this short and current so later phases do not re-scan the whole project.

## Phase 00 verified baseline

| Area | Current location | Notes |
|---|---|---|
| Root workspace | `package.json`, `pnpm-workspace.yaml` | pnpm workspace includes `artifacts/api-server`, `artifacts/localai-control-center`, and `lib/*`. Root scripts now include `verify:baseline` and `verify:jarvis`. |
| API server | `artifacts/api-server/src/app.ts`, `artifacts/api-server/src/index.ts` | Express 5 app with `/api`, `/v1`, and `/api/v1` mounts. Boot starts DB hydration, thought log, task queue, state orchestrator, foreground watcher, distributed heartbeat, tray/STT sidecars, and model catalog sync. |
| Route registry | `artifacts/api-server/src/routes/index.ts` | Registered modules: health, stack, models, workspace, system, updates, continue, studios, remote, chat, sessions, filebrowser, context, intelligence, integrations, usage, updater, repair, kernel, observability, tasks, rollback, stt, tts, rag, web, benchmark, pinboard, token-budget, timetravel, plugins, worldgui, foundation, approvals, runtime-mode, provider-policy. Extend this registry rather than creating a duplicate router. |
| OpenAI-compatible API | `artifacts/api-server/src/routes/openai.ts`, `artifacts/api-server/src/lib/openai-compat.ts` | Mounted at `/v1` and `/api/v1`; tests live in `artifacts/api-server/tests/openai-compat.test.ts`. |
| Database/schema | `artifacts/api-server/src/db/schema.ts`, `artifacts/api-server/src/db/migrate.ts` | SQLite/Drizzle tables cover chat sessions/messages, settings, service policies, capability state, role assignments, usage, thought log, workspace registry, model pulls, audit log, refactor plans/jobs, async jobs, benchmarks, pinboard, token budgets, plus foundation tables for workspace roots, local profiles, integration/plugin state, permission policies, approval requests, durable jobs/events, audit events, artifacts, Phase 17A Digital Twin entity/relationship exports, Phase 18 automotive vehicle/case/action exports, and Phase 21 recovery backup/restore plan exports. Runtime mode uses existing `app_settings`; service policies use `service_policies`. |
| Model routing/lifecycle | `artifacts/api-server/src/lib/model-orchestrator.ts`, `artifacts/api-server/src/lib/model-roles-service.ts`, `artifacts/api-server/src/lib/model-lifecycle.ts`, `artifacts/api-server/src/config/models.config.ts`, `artifacts/api-server/src/routes/models.ts` | Ollama/local gateway flow with SQLite role assignments, lifecycle/capability registry, backend profiles, approval-required model mutation proposals, VRAM guard concepts, install/stop/status routes, and benchmark route. Chat routing must not fall through to embedding-only models. |
| Safety/permissions | `artifacts/api-server/src/lib/route-guards.ts`, `artifacts/api-server/src/lib/command-sanitizer.ts`, `artifacts/api-server/src/routes/system.ts`, `artifacts/api-server/tests/permission-routes.test.ts`, `artifacts/api-server/tests/route-guard-coverage.test.ts`, `artifacts/api-server/tests/security.test.ts` | Local browser guard, distributed auth middleware, command sanitizer, execution/delete/update permission tests, and strict local mode fetch blocking in `app.ts`. |
| Tasks/jobs | `artifacts/api-server/src/lib/task-queue.ts`, `artifacts/api-server/src/lib/platform-foundation.ts`, `artifacts/api-server/src/routes/tasks.ts`, foundation tables in `schema.ts` | Existing async job queue plus durable job foundation. Phase 03 added checkpoint JSON, retry count, result/error/timestamps, pause/resume/cancel routes, and restart hydration. Later workers should reuse this instead of adding another queue. |
| Runtime modes/service policies | `artifacts/api-server/src/lib/runtime-mode.ts`, `artifacts/api-server/src/routes/runtime-mode.ts`, `artifacts/api-server/tests/runtime-mode.test.ts` | Phase 01 APIs: `GET /api/runtime-mode`, `POST /api/runtime-mode/set`, `GET /api/service-policies`, `POST /api/service-policies/:id/update`, `POST /api/emergency-stop`. Reuses settings, service policies, model orchestrator, task queue, thought log, and audit events. |
| Provider policy | `artifacts/api-server/src/lib/provider-policy.ts`, `artifacts/api-server/src/routes/provider-policy.ts`, `artifacts/api-server/tests/provider-policy.test.ts` | Phase 02 APIs: `GET /api/provider-policy`, `POST /api/provider-policy/evaluate`, `PUT /api/provider-policy/providers/:id`, `POST /api/provider-policy/providers/:id/test`. Reuses encrypted config, audit events, thought log, usage metrics, and Settings UI. Ollama/local gateway remain default/local. |
| Approval queue | `artifacts/api-server/src/lib/approval-queue.ts`, `artifacts/api-server/src/routes/approvals.ts`, `artifacts/api-server/tests/approval-queue.test.ts` | Phase 03 APIs: `GET/POST /api/approvals`, `POST /api/approvals/:id/approve`, `deny`, `cancel`. Approval requests link to durable jobs, hash payloads, log decisions, deny Tier 5/P5 actions, and verify approved payloads before execution. |
| Self-maintainer/updater safety | `artifacts/api-server/src/lib/self-maintainer.ts`, `artifacts/api-server/src/routes/updater.ts`, `routes/updates.ts`, `routes/repair.ts`, `routes/system.ts`, `routes/chat.ts`, `artifacts/api-server/tests/self-maintainer.test.ts` | Phase 06 APIs: `GET /api/updater/self-maintainer`, `POST /api/updater/self-maintainer/radar`, `POST /api/updater/self-maintainer/proposals`, and `POST /api/updater/self-maintainer/actions/propose`. Reuses updater/repair/model lifecycle/approval/durable job/audit/replay sources. Update/repair/self-improvement actions are proposal-first, approval-gated, rollback/test-aware, no direct-main apply, and Gaming Mode mutation blocked. |
| Rollback/audit | `artifacts/api-server/src/lib/snapshot-manager.ts`, `artifacts/api-server/src/routes/rollback.ts`, `audit_log`, `audit_events` tables | File edits already have backup/audit concepts; self-update and coding phases must reuse this. |
| Packaging/recovery/DR | `artifacts/api-server/src/lib/packaging-recovery.ts`, `artifacts/api-server/src/routes/system.ts`, `scripts/backup-config.mjs`, `scripts/restore-config.mjs`, `scripts/health-check.mjs`, `scripts/emergency-stop.mjs`, `scripts/gaming-mode.mjs`, `artifacts/api-server/tests/packaging-recovery.test.ts` | Phase 21 source of truth for metadata-only backup manifests, restore dry-run validation, approval-gated restore proposals, install-plan status, and safe helper scripts. Reuses existing updater/rollback/runtime mode/approval/durable job/audit systems. Real destructive restore remains not_configured by default; optional backup/install providers report not_configured. |
| RAG/web/memory | `artifacts/api-server/src/lib/rag.ts`, `artifacts/api-server/src/routes/rag.ts`, `artifacts/api-server/tests/rag.test.ts`, `web.ts`, `pinboard.ts`, `context.ts` | Phase 08A keeps `lib/rag.ts` as the RAG source of truth. Default ingestion is the existing built-in parser; default vector store is existing local hnswlib. `GET /api/rag/status`, source/chunk inspector routes, hash-based re-indexing, stale source cleanup, citation metadata, and not_configured optional MarkItDown/Docling/OCR/LanceDB/Qdrant statuses are attached to the same surface. Chat `/pin` and streaming personal-memory retrieval still use this RAG library. |
| Voice/vision/studios | `artifacts/api-server/src/routes/stt.ts`, `tts.ts`, `studios.ts`, `artifacts/api-server/src/lib/studio-pipeline.ts`, `artifacts/api-server/src/lib/maker-studio.ts`, `artifacts/localai-control-center/src/pages/Studios.tsx` | STT sidecar, Piper-style TTS route, and Studio workflows exist; preserve manifest-driven Studio generation. Phase 13A adds Maker Studio under existing Studios routes/UI with local project/material/CAD metadata, safety policy badges, optional maker integration status, and proposal-only physical actions. Phase 13B extends the same Maker source of truth with proposal-only FreeCAD/CAD-as-code/KiCad provider status, metadata-only design proposals, and a CAD Engineer panel in the existing Maker tab; no CAD/PCB/cloud/hardware execution exists. Phase 13C extends the same Maker source with slicer/printer/material/monitoring provider status, metadata-only dry-run slicing proposals, approval-gated queue/start/heater/motor proposals, material checks that can block queue proposals, Obico not_configured/degraded monitoring, and a 3D Print Workflow panel; no slicer/printer/API/monitoring/hardware execution exists. Phase 13D extends the same Maker source with CAM/CNC/laser/electronics provider status, metadata-only setup sheets, CAM/toolpath approval gates, manual-only G-code/motion/spindle/laser/firmware/relay/serial/USB gates, and a CNC/Laser/Bench Safety panel; no live toolpath, machine API, serial/USB, electronics bench, or hardware execution exists. Phase 19 adds a Robotics Lab tab in the same Studios page with 3 lazy SQLite tables (`robotics_robot_profiles`, `robotics_sim_plans`, `robotics_action_proposals`), PHASE_19_BLOCKED_ACTIONS/MANUAL_ONLY_ACTIONS Sets, and 9 not_configured optional providers; no physical hardware execution exists. |
| Local builder / self-build | `artifacts/api-server/src/lib/local-builder.ts`, `artifacts/api-server/src/routes/intelligence.ts` (local-builder routes appended), `artifacts/api-server/tests/local-builder.test.ts`, `artifacts/localai-control-center/src/pages/Studios.tsx` (Local Builder tab), `docs/context-packs/` (4 markdown files: core-architecture, safety-and-permissions, current-build-state, next-phase-template) | Phase 22: local-first approval-gated self-build workflow. Four model roles (fast_code, deep_code, reviewer, rag_embedding) with lazy SQLite persistence in `local_builder_profiles`/`local_builder_eval_history`. Hard limits cloudEscalationEnabled=false, selfModificationAllowed=false, requireApprovalForEdits=true are TypeScript literal types. `proposeBuildTask()` requires tier3_file_modification approval; shell metacharacter phaseIds/taskSummaries and own-source targetFiles trigger hardBlocked=true. Four local evals (repo_summary, safe_patch_plan, unsafe_action_detection, ledger_update) run with usedNetwork=false. Routes: `GET/PUT /intelligence/local-builder/{status,profiles,profiles/:role,context-packs,context-packs/:name}`, `POST /intelligence/local-builder/{build/propose,eval/run}`, `GET /intelligence/local-builder/eval/history`. Local Builder tab in Studios UI. |
| Integrations/plugins | `artifacts/api-server/src/routes/integrations.ts`, `plugins.ts`, `plugins/`, `docs/JARVIS_EXTERNAL_PROJECT_WATCHLIST.md` | Existing catalog and plugin manifest surface. Phase 07A routes integration install/start/update through the tool firewall with `executed: false`; Phase 07B adds Docker MCP Gateway status/profile/proposal surfaces under `/api/tools`; Phase 07C adds OpenClaw/NemoClaw gateway status/profile/config/skill/action proposal surfaces under `/api/tools`. Phase 13A-D maker/CAD/print/machine references keep authoritative provider status in `lib/maker-studio.ts` as proposal-only/manual-only not_configured/disabled. Phase 16 represents SOC provider status inside `lib/homelab-architect.ts`. Phase 18 adds automotive provider references for python-OBD, ELM327 emulator, SavvyCAN, OVMS, and ACES log import while authoritative status/actions live in `lib/automotive-diagnostics.ts` as not_configured/disabled/manual_only. New external projects must be recorded in the watchlist before implementation. |
| Tool registry/firewall | `artifacts/api-server/src/lib/tool-registry.ts`, `artifacts/api-server/src/lib/docker-mcp-gateway.ts`, `artifacts/api-server/src/lib/claw-gateway.ts`, `artifacts/api-server/src/routes/plugins.ts`, `artifacts/api-server/tests/tool-registry.test.ts`, `artifacts/api-server/tests/docker-mcp-gateway.test.ts`, `artifacts/api-server/tests/claw-gateway.test.ts` | Phase 07A source of truth projects existing plugins, integration catalog entries, and planned disabled MCP/browser/desktop/physical tool records into one fail-closed policy layer. Phase 07B attaches Docker MCP Gateway as an optional isolation/profile target with hidden-by-default allowlisted tools, source trust metadata, blockSecrets/blockNetwork defaults, and dry-run proposals. Phase 07C attaches OpenClaw/NemoClaw as optional future gateways with skill lifecycle/quarantine/review states, source trust metadata, external-message approval, and dry-run/proposal-only install/update/action paths. `/api/tools` routes expose inspect, enable/disable, dry-run, execute/proposal, Docker MCP status/profile/config/run proposal, and OpenClaw/NemoClaw status/profile/config/skill/action proposal checks; no real Docker/MCP/OpenClaw/NemoClaw adapter execution exists. |
| WorldGUI/desktop | `artifacts/api-server/src/routes/worldgui.ts`, `artifacts/api-server/src/lib/windows-system.ts`, `scripts/windows/LocalAI.Tray.ps1` | Windows/desktop automation exists and must remain approval-gated. Phase 01 blocks physical desktop action execution while Emergency Stop is active. Browser work should prefer Playwright later, with WorldGUI fallback. |
| Frontend shell | `artifacts/localai-control-center/src/App.tsx`, `src/api.ts`, `src/pages/*`, `src/components/PermissionNotice.tsx` | React 19/Vite 7/wouter/Tailwind control center pages: Dashboard, Chat, Models, Workspace, Studios, Remote, Operations, Integrations, Diagnostics, Logs, Cleanup, Settings. Phase 01 added the Operations Runtime tab; Phase 02 added the Settings local-first provider policy section; Phase 03 added Operations Approvals tab and Chat approval queuing; Phase 05 added the Models Lifecycle tab; Phase 06 added Operations Maintainer tab and proposal wording; Phase 07A-C added Tool Registry/Docker MCP/OpenClaw/NemoClaw surfaces; Phase 08A added Workspace RAG; Phase 13A-D added Maker/CAD/print/machine panels; Phase 17A added `/digital-twin`; Phase 18 added `/automotive`; Phase 21 adds an Operations Recovery tab via `recoveryApi` using existing card/pill/form/action-row style. UI phases must preserve this style. |
| HomeLab/Home SOC | `artifacts/api-server/src/lib/homelab-architect.ts`, `artifacts/api-server/src/routes/homelab.ts`, `artifacts/localai-control-center/src/pages/HomeLab.tsx`, `artifacts/api-server/tests/homelab-architect.test.ts`, `homelab-config-pipeline.test.ts`, `homelab-soc.test.ts` | HomeLab remains the source of truth for network inventory, config proposal/validation/apply safety, and Phase 16 Home SOC. SOC uses lazy SQLite `homelab_soc_alerts` and `homelab_soc_remediation_proposals`, `/api/homelab/soc/*` routes, and the existing HomeLab page. Providers (Wazuh, Zeek, Suricata, OPNsense IDS/IPS, Pi-hole, AdGuard, LibreNMS, Zabbix, Netdata, Uptime Kuma, osquery) are not_configured by default. Remediation is read-only/proposal/approval-gated/not_configured; packet capture is blocked; no real security/network API calls or infrastructure mutations execute. |
| Digital Twin | `artifacts/api-server/src/lib/digital-twin.ts`, `artifacts/api-server/src/routes/context.ts`, `artifacts/api-server/src/db/schema.ts`, `artifacts/localai-control-center/src/pages/DigitalTwin.tsx`, `artifacts/api-server/tests/digital-twin.test.ts` | Phase 17A local graph source of truth. Lazy SQLite `digital_twin_entities` and `digital_twin_relationships` store metadata and source refs into Evidence Vault/RAG, HomeLab, Home SOC, Maker Studio, Edge Nodes, Home Autopilot, vehicles, tools, and projects. `/api/context/digital-twin/*` routes expose status, CRUD/detail/archive, relationship create/delete-as-deleted, search, and action-safety. Unknown data remains unknown/proposed/not_configured; AI/inferred links require provenance; physical action checks delegate to existing safety policies and execute nothing. |
| Automotive diagnostics | `artifacts/api-server/src/lib/automotive-diagnostics.ts`, `artifacts/api-server/src/routes/context.ts`, `artifacts/api-server/src/routes/integrations.ts`, `artifacts/api-server/src/db/schema.ts`, `artifacts/localai-control-center/src/pages/Automotive.tsx`, `artifacts/api-server/tests/automotive-diagnostics.test.ts` | Phase 18 local Master Tech source of truth. Lazy SQLite `automotive_vehicle_profiles`, `automotive_diagnostic_cases`, and `automotive_action_proposals` store vehicle profiles, Foxbody build facts, symptom/DTC intake, diagnostic plans, repair logs, and action proposals. `/api/context/automotive/*` routes expose status/providers/vehicles/cases/actions. Optional python-OBD/ELM327/SavvyCAN/OVMS/ACES/CAN/external-data providers report not_configured/disabled; vehicle actions are not_configured, approval_required, or manual_only with `executed:false`. |
| Tests | `artifacts/api-server/tests/*`, `artifacts/localai-control-center/tests/*` | API tests cover security, OpenAI compatibility, route guards, permissions, foundation. UI tests cover API client/error and permission notice/page rendering. |
| Verification scripts | `scripts/verify-localai-baseline.mjs`, `scripts/verify-jarvis.mjs`, `scripts/jarvis/verify-build-kit.mjs`, shell/PowerShell baseline wrappers, Phase 21 recovery helper scripts | Baseline and recovery helper scripts must stay offline/local and not require Ollama, Docker, Python, network, GPU, service installs, startup changes, firewall changes, PATH edits, or destructive restore. |
| Jarvis docs | `docs/JARVIS_*.md`, `phase-prompts/`, `prompts/` | Phase control, acceptance contract, local handoff, requirements traceability, expert modes, source verification, UI guard, and external watchlist. |

## Update rule

Every phase that adds, renames, or meaningfully changes files/routes/libs/UI pages must update this index with concise file paths and ownership notes.
