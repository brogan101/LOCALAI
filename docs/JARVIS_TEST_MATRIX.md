# Jarvis Test Matrix

This file tracks what must be tested after each phase. Update it every phase.

## Baseline commands

| Check | Command | Required when | Last result | Blockers | Next action |
|---|---|---|---|---|---|
| Build-kit verifier | `node scripts/jarvis/verify-build-kit.mjs` | Phase 00 and kit/context changes | Passed 2026-05-01 during Phase 18 closeout | None | Keep running after build-kit doc/script edits. |
| Install dependencies | `pnpm install` | first checkout setup or dependency drift | Not run in Phase 00 verification audit | None observed; lockfile install was not required for docs/script-only verification | Run if dependencies are missing or package metadata changes. |
| Install lockfile strict | `pnpm install --frozen-lockfile` | dependency/package changes | Not run in Phase 00 verification audit | None observed; previous audit reported `pnpm install --frozen-lockfile --offline` passed after lockfile repair | Run on dependency changes or release validation. |
| Typecheck | `pnpm -r typecheck` | every phase unless environment-blocked | Passed 2026-05-01 during Phase 22 closeout | None | Keep as required closeout check. |
| Full tests | `pnpm test` | every phase unless environment-blocked | Passed 2026-05-01 during Phase 22 closeout, including `local-builder.test.ts` (21 assertions) | None | Keep as required closeout check. |
| API tests | `pnpm --filter api-server test` | API/backend changes | Passed via `pnpm test` 2026-05-01 during Phase 22 closeout | None | Run directly for backend-only debugging when useful. |
| UI tests | `pnpm --filter localai-control-center test` | frontend changes | Passed 2026-05-01 during Phase 22 closeout, including `test:ui-integration` and `test:api` scripts | None | Run directly for frontend-only debugging when useful. |
| UI build | `pnpm --filter localai-control-center build` | frontend/build changes | Passed 2026-05-01 during Phase 22 closeout; `Studios-NYszjgNe.js` emitted | None | Run on UI/build changes. |
| Baseline verifier | `pnpm run verify:baseline` | phase closeout | Passed 2026-04-25 | None | Keep as Phase 00+ context check. |
| Jarvis verifier | `pnpm run verify:jarvis` | phase closeout | Passed 2026-04-29 during Phase 08A closeout | None | Keep as Phase 00+ context check. |
| API live startup | `pnpm --filter api-server start` / externally managed API server | Phase 00.5+ runtime repair | Passed 2026-04-25 by manual Windows host verification: API server stayed running. Earlier Codex duplicate-start attempt failed with `listen UNKNOWN`, now classified as a Codex execution-context limitation. | None blocking | Use manual host/browser or a working shell context for future live proof. |
| API health probe | `http://127.0.0.1:3001/api/health` | Phase 00.5+ runtime repair | Passed 2026-04-25 by manual Windows/browser verification. Codex shell Node/curl probes still failed with `connect UNKNOWN` / service-provider socket errors. | None blocking | Treat Codex shell URL probing as limited until that execution context is repaired. |
| UI live startup | `pnpm --filter localai-control-center dev` / externally managed Vite dev server | Phase 00.5+ runtime repair | Passed 2026-04-25 by manual Windows host verification: UI dev server stayed running. | None blocking | Use manual host/browser or a working shell context for future live proof. |
| UI browser probe | `http://127.0.0.1:5173` | Phase 00.5+ runtime repair | Passed 2026-04-25 by manual Windows/browser verification. Codex shell Node/curl probes still failed with `connect UNKNOWN` / service-provider socket errors. | None blocking | Treat Codex shell URL probing as limited until that execution context is repaired. |
| Dependency audit | `pnpm audit --prod` | security/release phases | Passed 2026-04-29 after upgrading API `diff` to `8.0.3` | None | Keep running on dependency changes or release validation. |

## Degraded-mode cases to keep visible

| Case | Expected behavior | Current proof | Blockers | Next action |
|---|---|---|---|---|
| Ollama offline | Show offline/unavailable; no app crash. | README and audit describe graceful degradation; live retest blocked by host sockets. | B-001, B-002 | Retest after live startup works. |
| Docker offline | Optional integrations should show not installed/offline. | Not verified in Phase 00. | None yet | Verify in the relevant integration/MCP phase. |
| Git unavailable | Coding/update flows should block visibly. | Not verified in Phase 00. | None yet | Verify in maintainer/coding-agent phases. |
| Node/pnpm mismatch | Verifiers and app should report exact version issue or approved fallback. | `runtime-diagnostics.test.ts` verifies Node `v20.20.2` produces a blocked browser-tooling diagnostic when no fallback is considered; live diagnostic now reports bundled Node `v24.14.0` fallback as compatible. | None | Keep `LOCALAI_BROWSER_NODE_PATH`/bundled fallback detection explicit; do not fake success if fallback is missing or below `22.22.0`. |
| Python missing/broken | STT sidecar should publish warning, not block API boot. | Phase 00.5 added runtime diagnostic recording for STT spawn/version failures. | B-001 blocks live confirmation | Retest after API startup works. |
| NVIDIA telemetry unavailable | VRAM guard should fail soft. | `runtime-diagnostics.test.ts` still verifies pnputil display parsing; live host now returns NVML telemetry from `nvidia-smi` for RTX 5070 driver 596.21 with 12227 MiB total VRAM. | None | Keep pnputil/safe-mode fallback for future driver regressions. |
| No internet/API keys | Core local operation should still work. | `provider-policy.test.ts` verifies Ollama remains default, cloud providers are disabled/not_configured without keys, and provider tests use no network. Manual host/browser live API/UI proof from Phase 00.5 remains the live runtime proof. | None blocking | Continue to keep cloud provider tests mocked/disabled unless real credentials are intentionally configured. |
| Gaming mode active | Heavy services should stop or not start. | `runtime-mode.test.ts` verifies Gaming mode returns action/skipped evidence without requiring Docker, Ollama, or cloud; implementation unloads running Ollama models when reachable and pauses heavy queued jobs. | None blocking | Live-host behavior can be smoke-tested from the Operations Runtime tab when desired. |
| Emergency Stop active | Models unload where safe, queued work stops where safe, physical actions are denied, and audit/thought-log entries are written. | `runtime-mode.test.ts` verifies persisted EmergencyStop mode and physical-action denial; route hooks block system/WorldGUI physical action execution. | None blocking | Add per-service safe adapters before auto-executing managed stop commands. |
| Approval required | Powerful command/edit actions should queue approval and not execute until an approved matching payload hash is supplied. | `approval-queue.test.ts` verifies Tier 3 diff/rollback requirements, Tier 4 approval requirement, Tier 5/P5 denial, audit logging, and an unapproved command that does not create its marker file. | None blocking | Later phases must route new automation through `approval_requests` and `durable_jobs`. |
| Mission replay missing data | Replay should mark unavailable linked records as missing, not infer fake events. | `mission-replay.test.ts` inserts an approval referencing an unavailable durable job and verifies `dataStatus: "missing"`. | None blocking | Later phases should preserve this explicit missing-data contract. |
| Secret/private data in replay | Replay must redact secrets, tokens, cookies, raw prompts, and private file content-style payloads. | `mission-replay.test.ts` verifies API key/token/cookie/raw prompt fields are redacted and non-sensitive context remains visible. | None blocking | Extend redaction keys when new sensitive payload types are added. |
| Local eval harness | Evals must run without cloud API keys, paid APIs, or live third-party services. | `pnpm run eval:jarvis` passed on 2026-04-26 using the local mission replay eval harness; report declares `localOnly: true`, `networkUsed: false`, and `externalProvidersRequired: false`. | None blocking | Keep future evals deterministic and mock/not_configured when integrations are unavailable. |
| Model lifecycle approval gate | Model pull/load/unload/delete/replace must not execute without explicit approval. | `model-lifecycle.test.ts` verifies pull/delete routes return `202 approvalRequired`, create proposals, leave `jobId` absent before approval, and set `autoDeletesOldModel: false`. | None blocking | Future model executors must call `verifyModelActionApproval` before mutation. |
| Model replacement safety | Replacement suggestions must keep old models, require eval proof, preserve role capability, and never auto-pull/delete. | `model-lifecycle.test.ts` verifies replacement proposals are blocked without eval proof and always retain old model with `autoDeletesOldModel: false` and `autoPullsModel: false`. | None blocking | Later eval packs may add real role-specific scoring, but cannot bypass these gates. |
| Optional model backends | Optional providers/backends must not be required for startup/tests. | `model-lifecycle.test.ts` verifies Ollama remains default and LM Studio is disabled/not_configured unless configured; provider-policy rules keep missing cloud providers non-blocking. | None blocking | Keep optional backends profile-only until explicitly configured and approved. |
| Self-maintainer dry-run updates | Update radar should run locally without paid APIs, direct mutation, or lockfile/package changes. | `self-maintainer.test.ts` verifies dry-run/proposal state, `networkUsed: false`, required tests/rollback plans, source trust status, and unchanged `pnpm-lock.yaml`. | None blocking | Future update providers must preserve dry-run/proposal behavior and lockfile verification. |
| Self-maintainer approval gate | Update, repair, test, rollback, and self-improvement actions must not execute without approval. | `self-maintainer.test.ts` verifies unapproved actions return approval-required proposals, `/updater/update` launches nothing, and chat `prepare patch` queues approval without code changes. | None blocking | Future executors must call `verifySelfMaintainerApproval` before mutation. |
| Direct main apply blocked | No update may apply directly to `main`. | `self-maintainer.test.ts` verifies `proposeSelfMaintainerAction({ action: "apply", currentBranch: "main" })` is blocked and `applied: false`. | None blocking | Real apply adapters must require a branch/staged proposal. |
| Gaming Mode maintainer guard | Gaming Mode should allow read-only/dry-run update checks only. | `self-maintainer.test.ts` verifies non-read-only maintainer mutation proposals are blocked in Gaming Mode while dry-run checks remain proposal-only. | None blocking | Keep update/test/apply adapters disabled in Gaming Mode unless read-only. |
| Self-maintainer source safety | Unknown or unverified update sources must be blocked/not_configured and secrets must not be logged. | `self-maintainer.test.ts` verifies unknown sources are blocked, optional release checks are not_configured, and token/password-like values are redacted from proposals/chat command output. | None blocking | Add new source adapters only after watchlist/config approval and redaction review. |
| Tool registry/firewall | Tool calls must fail closed unless registered, configured, enabled, scoped, runtime-compatible, sandbox-satisfied, and approved when required. | `tool-registry.test.ts` verifies unregistered/not_configured blocks, missing permissions, approval-required and denied calls with `executed: false`, runtime blocks, high-risk disabled defaults, secret redaction, audit/replay records, and `/tools/not-real/execute` returning `not_configured`. | None blocking | Future MCP/tool executors must reuse `tool-registry.ts` and add durable approved adapters without weakening fail-closed behavior. |
| Docker MCP Gateway safety | Docker unavailable must not break startup; Docker MCP tools must stay hidden unless allowlisted, block unsafe permissions, and use dry-run/proposal behavior. | `docker-mcp-gateway.test.ts` verifies not_configured/degraded Docker status, hidden profile allowlists, blockSecrets/blockNetwork defaults, unknown/community source blocks, approval-required Docker tool calls, denied execution prevention, and secret redaction. | None blocking | Future Docker MCP executors must verify approved payloads and preserve profile/resource/mount/env restrictions. |
| OpenClaw/NemoClaw gateway safety | Missing gateways must not break startup; unknown/quarantined/rejected skills must not execute; external communication and install/update behavior must be proposal/approval gated. | `claw-gateway.test.ts` verifies missing gateways report not_configured, OpenClaw/NemoClaw records attach to the existing tool firewall, visible registry hides non-allowlisted gateways, unregistered/unknown/quarantined/rejected actions are blocked, approved skills still require declared permissions, external messages require approval, denied actions do not execute, install/update stays dry-run/proposal, secrets/env values are redacted, and route proposals return `executed: false`. | None blocking | Future OpenClaw/NemoClaw executors must verify source allowlists, skill review state, matching approval payloads, and durable job sandboxing before any real action. |
| Professional RAG default and optional providers | Existing personal memory/RAG must keep working through local built-in parser + hnswlib; optional parsers/vector stores must not fake success. | `rag.test.ts` verifies hnswlib remains the default vector provider, built-in parser is available, MarkItDown/Docling/OCR/LanceDB/Qdrant report `not_configured`, unchanged sources are skipped by hash, changed sources re-index, stale chunks/sources are marked deleted, citation metadata is stored with missing page/section as `unavailable`, and private document contents/secrets are absent from RAG audit/thought logs. | None blocking | Verified; Phase 08B reuses this RAG source of truth for evidence search/ingestion. |
| Evidence Vault local-first defaults | Evidence records must be created, categorized, and searched without Paperless-ngx, Docker, cloud APIs, or network; Paperless-ngx must return `not_configured` when not configured. | `evidence-vault.test.ts` verifies record creation without Paperless, category storage and filtering, Paperless `not_configured` status, Paperless sync dry-run `proposalStatus: "not_configured"`, duplicate detection by hash (skip unchanged / reindex changed / mark deleted stale), privacy classification preservation, secret records blocked from RAG, evidence search via existing `rag.search()` path with `ragPath: "local_hnswlib"`, reminder proposals with `calendarIntegrationStatus: "not_configured"`, and private content / secret sentinel strings absent from log text. | None blocking | Keep evidence search delegating to the existing RAG path; never add a duplicate vector store for evidence. |
| Evidence Vault privacy and secret protection | Secret-classified evidence must never reach shared RAG collections; private document contents must not appear in diagnostics, logs, or test output. | `evidence-vault.test.ts` asserts `NOT_REAL_VIN` and `TOP_SECRET_NETWORK_PASSWORD` are absent from all thought-log/audit text emitted during ingest operations; secret records throw before calling `rag.ingest()`. | None blocking | Future evidence categories or classification changes must preserve this hard block; never weaken the secret-record RAG guard. |
| Maker Studio physical safety | Maker Studio must work without FreeCAD, CAD-as-code runtimes, KiCad, slicers, printers, CNC, laser, electronics bench, cloud APIs, Docker, Python, or network; physical actions must not execute by default. | `maker-studio.test.ts` verifies local-first status, not_configured maker integrations, project/material/CAD metadata persistence, simulate proposal-only behavior, print action approval-required but not_configured after approval, CNC manual-only auto-denial, and no private project data/secrets in Maker audit metadata. | None blocking | Phase 13B+ adapter work must preserve Phase 13A safety tiers and not add real machine execution without a later explicit approved executor workflow. |
| Maker CAD adapter safety | FreeCAD/CAD-as-code/KiCad/text-to-CAD adapter surfaces must work without FreeCAD, KiCad, Python CAD libraries, OpenSCAD, cloud APIs, Docker, network, or hardware; providers must not fake success. | `maker-cad-adapters.test.ts` verifies FreeCAD/CadQuery/build123d/OpenSCAD/KiCad report not_configured, gNucleus/BuildCAD cloud text-to-CAD providers report disabled, CAD-as-code and KiCad proposals are metadata-only, no external tool execution is attempted, fabrication/manufacturing actions are manual-only or blocked, proposal metadata includes target files/units/dimensions/constraints/assumptions/risk/validation, and secrets/private project data are absent from audit/artifact text. | None blocking | Phase 13C+ slicer/printer/CNC/electronics work must keep CAD provider execution disabled until a later explicit approved executor exists. |
| Maker 3D printer workflow safety | Slicer, printer, material inventory, and monitoring surfaces must work without OrcaSlicer, PrusaSlicer, SuperSlicer, OctoPrint, Moonraker/Klipper, Mainsail, Fluidd, FDM Monster, Spoolman, Obico, Docker, Python, network, cloud APIs, or hardware; providers must not fake success. | `maker-print-workflow.test.ts` verifies slicers/printers/Spoolman/Obico report not_configured/disabled, slicing proposals are dry-run/config-validation metadata only, no slicer/G-code/upload/API/monitoring/hardware action occurs, queue/start/heater/motor actions require approval or block, denied approvals do not execute, missing/unknown material blocks queue proposals, local-first/no-cost defaults remain, and printer tokens/private project data are absent from audit/artifact text. | None blocking | Phase 13D+ CNC/laser/CAM/electronics work must preserve approval/manual-only gates and never add real physical execution without explicit approved executor workflows. |
| Maker CNC/laser/CAM/electronics bench safety | CNC, laser, CAM, and electronics bench surfaces must work without FreeCAD Path/CAM, CNCjs, LinuxCNC, FluidNC, bCNC, LightBurn, KiCad electronics tools, InvenTree, serial/USB devices, Docker, Python, network, cloud APIs, machine APIs, or hardware; providers must not fake success. | `maker-machine-safety.test.ts` verifies missing CAM/CNC/laser/electronics providers report not_configured/disabled, setup sheets are metadata-only proposals, CAM/toolpath actions are approval-required, G-code send/motion/spindle/laser/firmware/relay/serial/USB actions are manual-only, denied approvals do not execute, no API/hardware call flags are set, local-first/no-cost defaults remain, and serial IDs/tokens/private project data are absent from audit/artifact text. | None blocking | Phase 14+ edge/home/shop work must preserve manual-only physical gates and never add real machine or device execution without explicit approved executor workflows. |
| Edge node degraded-mode safety | Missing/unconfigured edge nodes must return not_configured; gaming PC must always skip live health probe and return unknown health; camera frame capture must be permanently blocked regardless of node profile or capability config; shop relay/power control must be permanently manual_only; no home/shop/device API calls must occur during tests; device tokens, private endpoint URLs, and camera data must not appear in thought logs or audit metadata. | `edge-node.test.ts` verifies nodes with empty endpoints return `health: "not_configured"`, gaming PC node always returns `health: "unknown"` (probe skipped), unknown nodes return blocked/not_configured, `camera_frame_capture` capability is blocked/disabled in camera_nvr role, `shop_relay_control` capability is manual_only/disabled in shop_controller role, `evaluateEdgeAction` returns `executed: false` for all tiers including read_only, blocked, manual_only, and approval_required, thought log entries contain no raw IP endpoints, auth tokens, or passwords in metadata, and HTTP health-check route returns not_configured when no endpoint configured. | None blocking | Gaming PC `alwaysOn` hard limit and camera/relay hard limits must never be made configurable; `executed: false` must remain true for all edge action evaluations until a later explicit approved executor phase. |
| HomeLab Architect degraded-mode safety | All optional providers (NetBox, Nautobot, Proxmox, OPNsense, UniFi, Ansible, OpenTofu, Batfish) must default `not_configured` and never fake success; no blueprint may have `applied: true`; VLAN IDs outside 1–4094 must be rejected with a validation error; invalid CIDR subnet prefixes must be rejected; thought log must never include raw management IPs, serial numbers, gateway addresses, or credentials; empty inventory must return empty-but-valid blueprint with `applied: false` and `overallConfidence: "unknown"`; unknown-confidence records must trigger warning notes in blueprint. | `homelab-architect.test.ts` verifies all 8 providers `not_configured`, `validateVlanId` reject 0/4095, `upsertVlan` throw for invalid ID, `validateSubnetPrefix` reject non-CIDR/bad-octet, `upsertSubnet` throw for invalid prefix, blueprint `applied=false` always, blueprint distinguishes confirmed/proposed/unknown confidence, warning notes for unknown-confidence records, no real network/API calls during tests, thought log entries contain no raw IP addresses/credentials/private tokens. | None blocking | `applied: false` TypeScript literal type and all 8 provider `not_configured` defaults must never be changed; privacy log filter must be preserved; VLAN/subnet validation must not be weakened. |
| HomeLab config generation and apply safety | Config proposals must be draft/proposal/dry_run by default, validation-first, diff-first, backup/rollback-aware, approval-gated, and not_configured when providers are missing; denied applies must not execute. | `homelab-config-pipeline.test.ts` verifies local proposal creation without real providers, NetBox/Nautobot/Proxmox/OPNsense/UniFi/Ansible/OpenTofu/Docker Compose/Batfish not_configured/degraded status, static/simulated/unavailable/real validation distinctions, apply blocked before validation and without backup plan, firewall/DHCP/VLAN approval requirement, denied approval no-execution, approved missing provider returns not_configured, no real API call flags, audit/replay privacy, and `/homelab/config/*` routes. | B-009 deferred only | Future HomeLab executor phases must preserve approval payload verification, provider configuration checks, validation proof, backup/rollback proof, and redacted logs before any real infrastructure apply. |
| Home autopilot degraded-mode safety | Unconfigured HA/MQTT/devices must return not_configured and blocked, never fake success; unknown HA entities not in the allowlist must be blocked regardless of action; unknown MQTT topics not in the allowlist must be blocked for publish; camera frame capture, snapshot, recording changes, and stream access must be permanently blocked by `BLOCKED_HOME_ACTIONS` regardless of device profile; compressor and main power must be permanently `manual_only` regardless of profile; garage door, lock/unlock, heater, relay, vacuum clean-zone, and alarm actions must always require approval; all three eval functions must return `executed: false` with no execution side effects; HA auth tokens, MQTT broker credentials, camera frames, and private endpoint URLs must never appear in thought log or audit metadata. | `home-autopilot.test.ts` verifies: missing HA/MQTT profiles return null; status returns all not_configured flags false when nothing is registered; `evaluateHaAction` with no profile returns `blocked`; unknown entity (not in allowlist) is blocked; `read_state` on allowlisted entity returns `read_only`; `garage_door_open`/`lock_unlock` return `approval_required`; `camera_frame_capture` on HA entity returns `blocked`; `evaluateMqttPublish` with no profile returns `blocked`; unknown topic blocked; wildcard topic matching works; `evaluateDeviceAction` for unknown device returns `blocked`; garage door `open` returns `approval_required` (hard limit); compressor `start` returns `manual_only` (hard limit); `camera_nvr` `frame_capture` returns `blocked` (hard limit); robot vacuum `clean_zone` returns `approval_required`; thought log metadata entries contain no `authToken`/`endpoint`/`token`/`credentials`/`cameraFrame`/`brokerUrl`; HTTP routes return 400 for missing required fields and 404 for unknown devices. | None blocking | `BLOCKED_HOME_ACTIONS`, `MANUAL_ONLY_HOME_ACTIONS`, and `APPROVAL_REQUIRED_HOME_ACTIONS` Sets must never be reduced; `executed: false` TypeScript literal type must remain on `HomeActionEvalResult`; privacy log filter must never be removed; all providers must default `not_configured` until explicitly configured. |
| Browser automation hard limits | Credential entry, anti-bot evasion, and cookie capture must be permanently blocked regardless of profile configuration or approval; Playwright MCP missing must not break startup. | `playwright-browser.test.ts` verifies `not_configured` default status, `credentialEntryAllowed/antiBoEvasionAllowed/cookieStorageAllowed` hardcoded `false`, action tier classification for all blocked and approval-required tiers, domain block policy, firewall blocking credential/anti-bot/cookie actions via `evaluateBrowserFirewall()`, Phase 07A stub replaced by Phase 09A records, proposal URL redaction (no raw URL in proposal JSON), audit events do not contain SECRET/PASSWORD/TOKEN, HTTP status/profile/navigate-propose/action-propose routes work without Playwright MCP installed. | None blocking | Hard-limit fields must never be made configurable; credential/anti-bot/cookie block must be preserved in all future browser automation phases. |
| Desktop automation hard limits | Credential entry, keylogging, and sensitive-window screenshot capture must be permanently blocked regardless of profile configuration or approval; WorldGUI missing must not break startup; banking/password manager/security apps must be excluded by policy. | `desktop-automation.test.ts` verifies `not_configured` default status, `credentialEntryAllowed/keyloggerAllowed/screenshotSensitiveAllowed` hardcoded `false`, action tier classification for all blocked and approval-required tiers, excluded-app blocking for password managers/banking/security/system-admin apps, firewall blocking via `evaluateDesktopFirewall()`, Phase 07A stub replaced by Phase 09B records, proposal targetApp redaction (`[redacted-for-proposal]`), audit events do not contain SECRET/PASSWORD, HTTP status/profile/action-propose routes work without WorldGUI installed. | None blocking | Hard-limit fields must never be made configurable; credential/keylogger/sensitive-screenshot block must be preserved in all future desktop automation phases. |
| Coding agent hard limits | Self-modification, direct main apply, and destructive commands must be permanently blocked regardless of profile configuration or approval; file modification must require tier3 approval before execution; optional runtime adapters must return not_configured when not installed. | `coding-agent.test.ts` verifies `selfModificationAllowed/directMainApplyAllowed/destructiveCommandsAllowed` hardcoded `false`, hard limits survive `saveCodingAgentProfile` patches, action tier classification for all blocked and approval-required tiers, workspace path allowlist enforcement, firewall blocking via `evaluateCodingAgentFirewall()`, self-modification/direct-main-apply tool records disabled+hardBlocked, execute-refactor has `approvalRequirement=required`, all 5 optional adapters return `not_configured`, proposal creates tier3 approval with diff/rollback metadata, audit events created, HTTP status/profile/task-propose routes work, execute-refactor route returns 202+approvalRequired when no approvalId. | None blocking | Hard-limit fields must never be made configurable; approval gate on execute-refactor must never be bypassed; optional adapter stubs must not fake execution. |
| Voice/meeting degraded-mode safety | STT/TTS sidecars unavailable must fail soft (503/not_configured) without crashing the API; wake word detection must report not_configured when no engine is installed; always-on capture must remain permanently blocked; follow-up sends must be blocked without a matching tier4 approval; Screenpipe must report not_configured by default. | `voice-meeting.test.ts` verifies STT status returns `sttSidecarStatus: "not_configured"` when sidecar is unreachable, `wakeWordStatus: "not_configured"` always (no engine installed), `alwaysOnEnabled: false` always, `cloudSttEnabled: false` always, `cloudTtsEnabled: false` always, `screenpipeEnabled: false` always, `alwaysOnCaptureEnabled: false` hard limit survives policy save patches, `meetingFollowUpApprovalRequired: true` hard limit survives patches, `proposeFollowUpSend` always returns `approvalRequired: true` with a valid approvalId, and `captureMode` defaults to `"disabled"` before any profile is set. | None blocking | Hard-limit fields (alwaysOnCapture/cloudSTT/cloudTTS/screenpipe) must never be made configurable; follow-up auto-send path must not exist; captureMode must default to "disabled". |
| Business module adapter safety | CRM/support/calendar/social/email/SMS adapters must default disabled/not_configured, external sends/syncs must never execute in Phase 12A, lead outreach must create approval, and private lead/customer/message contents must not be logged. | `business-modules.test.ts` verifies the module registry and hard limits, default adapter statuses, disabled sync `executed:false`, enabled-but-unconfigured `not_configured`, lead draft approval creation, unapproved external sends returning approvalRequired, approved sends still not executing when adapters are unavailable, SQLite draft storage/listing, status source-of-truth metadata, and audit/mission replay text excluding raw private lead/customer/message secrets. | None blocking | Future business executor work must verify matching approval payloads, keep adapters scoped per service, redact private business contents, and never turn Phase 12A proposal/status surfaces into fake execution. |
| IT support script safety | Generated scripts must default to proposal/review/dry-run, include safety metadata, require approval for execution proposals, block/approval-gate dangerous admin commands, report optional integrations as not_configured/disabled, and keep secrets/private ticket data out of logs. | `it-support.test.ts` verifies all 8 workflows, optional Windows Event Log/AD-GPO/Fortinet/Ivanti/Exchange/script-executor statuses, generated PowerShell safety contract fields, missing metadata blocking, shared sanitizer blocking destructive IT patterns, manual-only script validation, approval-required script proposals, denied approval non-execution, approved proposal returning not_configured, local-first/no-cloud status, and audit text excluding private request contents/secrets. | None blocking | Future IT executor work must verify matching approval payloads, run only exact reviewed scripts in scoped/sandboxed mode, preserve backup/rollback/proof logs, and keep private tickets/logs/secrets redacted. |
| Robotics Lab physical hardware blocked | `execute_motion` and `navigate` must be permanently blocked regardless of approval tier, profile, or configuration; all 9 optional providers must return `not_configured`; `physicalHardwarePresent`, `simulationOnly`, `hardwareExecutionBlocked`, and `executed` must remain TypeScript literal types; `gripper_open/close`, `arm_move`, `firmware_flash`, `relay_toggle`, `serial_write` must remain `manual_only`; robot profile tokens/private config must not appear in thought logs or audit metadata. | `robotics-lab.test.ts` verifies: status `localFirst: true`; all 9 providers `not_configured`; robot profile `physicalHardwarePresent: false` literal; `listRobotProfiles` returns stored profile; `createSimPlan` returns `simulationOnly: true` and `hardwareExecutionBlocked: true` literals; `listSimPlans` filters by profileId; `execute_motion` returns `blocked`; `navigate` returns `blocked`; `gripper_open` returns `manual_only`; `arm_move` returns `manual_only`; `serial_write` returns `manual_only`; `sim_run` returns `simulation_only`; `read_state` returns `not_configured`; private token/auth string absent from audit metadata; `firmware_flash` returns `manual_only`; `plan_motion` creates an approval proposal. | None blocking | `PHASE_19_BLOCKED_ACTIONS` Set (`execute_motion`, `navigate`) must never be reduced; `MANUAL_ONLY_ACTIONS` Set must never be reduced; TypeScript literal types on `RoboticsStatus`, `RoboticsSimPlan`, and `RoboticsActionProposal` must never be changed to mutable or runtime-checked values; all 9 provider defaults must remain `not_configured` until explicitly configured. |
| UI status badge honest rendering | StatusPill must never render "active", "ready", or "online" for `not_configured`/`degraded`/`blocked`/`manual_only` states; UnavailableCard must never claim a component is active/ready/connected when reason is `not_configured` or `degraded`; unknown status strings must fall back gracefully without throwing; Dashboard must not expose raw approval payloads, `payloadHash`, or private values in rendered HTML even when approval data is seeded; LocalCloudBadge must visually distinguish local-only vs. cloud data paths; PhysicalTierBadge must use error color for `blocked`/`manual_only` and warn color for `approval_required`/`simulation_only`. | `ui-integration.test.tsx` 24 assertions verify: StatusPill states (not_configured muted, blocked error, approval_required warn + readable label, manual_only error + readable label, active success, dry_run warn, degraded warn), LocalCloudBadge (local=info "Local only"; cloud=warn "Cloud — data leaves machine"), PhysicalTierBadge (manual_only error, blocked error, approval_required warn, simulation_only warn), UnavailableCard (title + reason pill + hint; doesNotMatch active/ready/connected), Dashboard RuntimeMode card (mode text rendered), Dashboard pending approvals ("None pending" / "2 pending"), Dashboard updater ("No proposals" / "1 proposal"), Dashboard EmergencyStop ("Physical actions disabled"), no payloadHash/raw secrets in rendered HTML, unknown StatusPill falls back gracefully. | None blocking | StatusPill fallback branch (unknown status → muted/elevated) must be preserved; UnavailableCard must never show green/active state; Dashboard query keys must match API expectations: `["runtime-mode"]`, `["approvals-dash"]`, `["updater-dash"]`. |

## Phase-specific test log

| Phase | Files changed | Tests run | Result | Blockers | Next action |
|---|---|---|---|---|---|
| Phase 00 | `package.json`; `scripts/verify-localai-baseline.mjs`; `scripts/verify-jarvis.mjs`; `docs/JARVIS_CONTEXT_INDEX.md`; `docs/JARVIS_IMPLEMENTATION_LEDGER.md`; `docs/JARVIS_DECISIONS.md`; `docs/JARVIS_PHASE_MAP.md`; `docs/JARVIS_BLOCKERS.md`; `docs/JARVIS_TEST_MATRIX.md`; `docs/JARVIS_LOCAL_AI_HANDOFF.md`; `docs/JARVIS_EXTERNAL_PROJECT_WATCHLIST.md` | `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm run verify:baseline`; `pnpm run verify:jarvis` | Passed | B-001 through B-011 remain open | Phase 00.5 only after Phase 00 verification passes. |
| Phase 00 verification audit | `phase-prompts/PHASE_00_REPO_BASELINE_AND_CONTEXT_SYSTEM.md`; `scripts/jarvis/verify-build-kit.mjs`; `scripts/verify-localai-baseline.mjs`; `scripts/verify-jarvis.mjs`; `JARVIS_CODEX_PROMPT_PACK_v2.md`; `prompts/RUN_PHASE_00_NOW.md`; `prompts/PHASE_FILE_INDEX.md`; `docs/JARVIS_EXECUTION_GUIDE.md`; `docs/JARVIS_PHASE_MAP.md`; `docs/JARVIS_BLOCKERS.md`; `docs/JARVIS_TEST_MATRIX.md`; `docs/JARVIS_LOCAL_AI_HANDOFF.md`; `docs/LOCALAI_UPGRADE_IMPLEMENTATION_PLAN.md`; `docs/JARVIS_IMPLEMENTATION_LEDGER.md` | `node scripts/jarvis/verify-build-kit.mjs`; `pnpm run verify:baseline`; `pnpm run verify:jarvis`; `pnpm -r typecheck`; `pnpm test` | Passed | Existing B-001 through B-011 remain open; no new blockers found | Cleared to run Phase 00.5 next, but Phase 00.5 was not started. |
| Phase 00.5 runtime blocker repair attempt | `artifacts/api-server/src/lib/runtime-diagnostics.ts`; `artifacts/api-server/src/routes/health.ts`; `artifacts/api-server/src/app.ts`; `artifacts/api-server/src/lib/hardware-probe.ts`; `artifacts/api-server/src/index.ts`; `artifacts/api-server/tests/runtime-diagnostics.test.ts`; `artifacts/api-server/package.json`; `package.json`; `scripts/clean-workspace.mjs`; `LAUNCH_OS.ps1`; `phase-prompts/PHASE_00_5_REPAIR_CURRENT_RUNTIME_BLOCKERS.md`; `scripts/jarvis/verify-build-kit.mjs`; `scripts/verify-localai-baseline.mjs`; `scripts/verify-jarvis.mjs`; `prompts/PHASE_FILE_INDEX.md`; `docs/JARVIS_IMPLEMENTATION_LEDGER.md`; `docs/JARVIS_PHASE_MAP.md`; `docs/JARVIS_BLOCKERS.md`; `docs/JARVIS_TEST_MATRIX.md`; `docs/JARVIS_LOCAL_AI_HANDOFF.md` | `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter api-server start`; `pnpm --filter localai-control-center dev`; API/UI HTTP probes | NOT COMPLETE: static checks passed, live startup failed | B-001 and B-002 still block Phase 00.5 completion; B-003/B-004/B-006 mitigated but host/runtime repair remains | Continue Phase 00.5 only; do not run Phase 01. |
| Phase 00.5 corrected-root retest | `docs/JARVIS_BLOCKERS.md`; `docs/JARVIS_TEST_MATRIX.md`; `docs/JARVIS_IMPLEMENTATION_LEDGER.md`; `docs/JARVIS_LOCAL_AI_HANDOFF.md` | `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter api-server start`; `Invoke-WebRequest http://127.0.0.1:3001/api/health`; `pnpm --filter localai-control-center dev`; `Invoke-WebRequest http://127.0.0.1:5173` | NOT COMPLETE: static checks passed; API/UI live startup and HTTP probes failed | B-001 and B-002 still block Phase 00.5 completion | Continue Phase 00.5 only; do not run Phase 01. |
| Phase 00.5 live-only recheck | `docs/JARVIS_BLOCKERS.md`; `docs/JARVIS_TEST_MATRIX.md`; `docs/JARVIS_IMPLEMENTATION_LEDGER.md`; `docs/JARVIS_PHASE_MAP.md`; `docs/JARVIS_LOCAL_AI_HANDOFF.md` | `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `netstat -aon -p tcp`; Node `fetch`; Node `http.get`; `curl.exe -v --max-time 10 http://127.0.0.1:3001/api/health`; `curl.exe -v --max-time 10 http://127.0.0.1:5173` | NOT COMPLETE: static checks passed; live-only URL probes failed; `netstat` showed no listener for `:3001` or `:5173` from this shell | B-001 and B-002 still block Phase 00.5 completion | Continue Phase 00.5 only; do not run Phase 01. |
| Phase 00.5 manual host verification closeout | `docs/JARVIS_BLOCKERS.md`; `docs/JARVIS_TEST_MATRIX.md`; `docs/JARVIS_IMPLEMENTATION_LEDGER.md`; `docs/JARVIS_PHASE_MAP.md`; `docs/JARVIS_LOCAL_AI_HANDOFF.md` | Manual Windows/browser verification of `http://127.0.0.1:3001/api/health`; manual Windows/browser verification of `http://127.0.0.1:5173`; prior static checks: `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm run verify:baseline`; `pnpm run verify:jarvis` | COMPLETE: API/UI live URLs passed in actual Windows host/browser context; Codex shell probing remains a non-blocking execution-context limitation | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 01. |
| Phase 01 Gaming-PC-safe runtime modes | `artifacts/api-server/src/lib/runtime-mode.ts`; `artifacts/api-server/src/routes/runtime-mode.ts`; `artifacts/api-server/src/routes/index.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/src/lib/task-queue.ts`; `artifacts/api-server/src/app.ts`; `artifacts/api-server/src/routes/system.ts`; `artifacts/api-server/src/routes/worldgui.ts`; `artifacts/api-server/tests/runtime-mode.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Operations.tsx`; `phase-prompts/PHASE_01_GAMING_PC_SAFE_RUNTIME_MODES.md`; `scripts/jarvis/verify-build-kit.mjs`; Jarvis docs | `pnpm --filter api-server run test:runtime-mode`; `pnpm --filter api-server typecheck`; `pnpm --filter localai-control-center typecheck`; `pnpm -r typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm test` | COMPLETE: all required static checks and test suites passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 02. |
| Phase 02 Local-first provider policy | `artifacts/api-server/src/lib/provider-policy.ts`; `artifacts/api-server/src/routes/provider-policy.ts`; `artifacts/api-server/src/lib/secure-config.ts`; `artifacts/api-server/src/routes/index.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/src/routes/usage.ts`; `artifacts/api-server/tests/provider-policy.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/SettingsPage.tsx`; `phase-prompts/PHASE_02_LOCAL_FIRST_OPTIONAL_API_POLICY.md`; `scripts/jarvis/verify-build-kit.mjs`; `prompts/PHASE_FILE_INDEX.md`; Jarvis docs; `README.md` | `pnpm --filter api-server run test:provider-policy`; `pnpm -r typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: provider policy test passed with 21 assertions; all required static checks, full tests, and UI build passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 03. |
| Phase 03 Approval queue, permission tiers, and durable jobs | `artifacts/api-server/src/lib/approval-queue.ts`; `artifacts/api-server/src/lib/platform-foundation.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/src/routes/approvals.ts`; `artifacts/api-server/src/routes/tasks.ts`; `artifacts/api-server/src/routes/system.ts`; `artifacts/api-server/src/routes/index.ts`; `artifacts/api-server/src/app.ts`; `artifacts/api-server/src/lib/thought-log.ts`; `artifacts/api-server/tests/approval-queue.test.ts`; `artifacts/api-server/tests/foundation.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Chat.tsx`; `artifacts/localai-control-center/src/pages/Operations.tsx`; `phase-prompts/PHASE_03_APPROVAL_QUEUE_AND_DURABLE_JOBS.md`; `scripts/jarvis/verify-build-kit.mjs`; `prompts/PHASE_FILE_INDEX.md`; Jarvis docs | `pnpm --filter api-server run test:approval-queue`; `pnpm --filter api-server run test:foundation`; `pnpm --filter api-server run test:permission-routes`; `pnpm -r typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: approval queue test passed with 15 assertions; foundation test passed with 33 assertions; permission route test passed with 154 assertions; all required static checks, full tests, and UI build passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 04. |
| Phase 04 Observability, evals, mission replay, and proof harness | `artifacts/api-server/src/lib/mission-replay.ts`; `artifacts/api-server/src/routes/observability.ts`; `artifacts/api-server/src/routes/chat.ts`; `artifacts/api-server/tests/mission-replay.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Operations.tsx`; `package.json`; `phase-prompts/PHASE_04_OBSERVABILITY_EVALS_AND_MISSION_REPLAY.md`; `scripts/jarvis/verify-build-kit.mjs`; `scripts/verify-jarvis.mjs`; `prompts/PHASE_FILE_INDEX.md`; Jarvis docs | `pnpm --filter api-server run test:mission-replay`; `pnpm run eval:jarvis`; `pnpm -r typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm test`; `pnpm run verify:jarvis`; `pnpm --filter localai-control-center build` | COMPLETE: mission replay test passed with 25 assertions; local eval command passed; full tests, typecheck, build-kit verifier, Jarvis verifier, and UI build passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 05. |
| Phase 05 Unified AI gateway, model router, and model lifecycle manager | `artifacts/api-server/src/lib/model-lifecycle.ts`; `artifacts/api-server/src/routes/models.ts`; `artifacts/api-server/src/lib/provider-policy.ts`; `artifacts/api-server/src/lib/secure-config.ts`; `artifacts/api-server/tests/model-lifecycle.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Models.tsx`; `phase-prompts/PHASE_05_MODEL_ROUTER_AND_MODEL_LIFECYCLE.md`; `scripts/jarvis/verify-build-kit.mjs`; `prompts/PHASE_FILE_INDEX.md`; Jarvis docs | `pnpm --filter api-server run test:model-lifecycle`; `pnpm --filter api-server typecheck`; `pnpm --filter localai-control-center typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build`; `pnpm run verify:jarvis` | COMPLETE: lifecycle targeted test passed with 32 assertions; full tests, typecheck, build-kit verifier, Jarvis verifier, and UI build passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 06. |
| Phase 06 Self-updating and self-improving Jarvis maintainer | `artifacts/api-server/src/lib/self-maintainer.ts`; `artifacts/api-server/src/routes/updater.ts`; `artifacts/api-server/src/routes/updates.ts`; `artifacts/api-server/src/routes/repair.ts`; `artifacts/api-server/src/routes/system.ts`; `artifacts/api-server/src/routes/chat.ts`; `artifacts/api-server/tests/self-maintainer.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Operations.tsx`; `artifacts/localai-control-center/src/pages/Dashboard.tsx`; `artifacts/localai-control-center/src/pages/Diagnostics.tsx`; `phase-prompts/PHASE_06_SELF_UPDATING_SELF_IMPROVING_MAINTAINER.md`; `scripts/jarvis/verify-build-kit.mjs`; `prompts/PHASE_FILE_INDEX.md`; Jarvis docs | `pnpm --filter api-server run test:self-maintainer`; `pnpm --filter api-server run test:route-guards`; `pnpm --filter api-server run test:permission-routes`; `pnpm --filter api-server run test:model-lifecycle`; `pnpm --filter api-server run test:mission-replay`; `pnpm --filter api-server typecheck`; `pnpm --filter localai-control-center typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build`; `pnpm run verify:jarvis` | COMPLETE: self-maintainer targeted test passed with 66 assertions; full tests, typecheck, build-kit verifier, Jarvis verifier, and UI build passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 07A. |
| Phase 07A MCP tool registry and tool firewall foundation | `artifacts/api-server/src/lib/tool-registry.ts`; `artifacts/api-server/src/routes/plugins.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/tests/tool-registry.test.ts`; `artifacts/api-server/tests/route-guard-coverage.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Integrations.tsx`; Jarvis docs | `pnpm --filter api-server run test:tool-registry`; `pnpm --filter api-server run test:route-guards`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build`; `pnpm run verify:jarvis` | COMPLETE: tool-registry targeted test passed; route-guard test passed with 42 assertions; full tests, typecheck, build-kit verifier, Jarvis verifier, and UI build passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 07B. |
| Phase 07B Docker MCP Gateway integration | `artifacts/api-server/src/lib/docker-mcp-gateway.ts`; `artifacts/api-server/src/lib/tool-registry.ts`; `artifacts/api-server/src/routes/plugins.ts`; `artifacts/api-server/tests/docker-mcp-gateway.test.ts`; `artifacts/api-server/tests/tool-registry.test.ts`; `artifacts/api-server/tests/route-guard-coverage.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Integrations.tsx`; Jarvis docs | `pnpm --filter api-server run test:docker-mcp`; `pnpm --filter api-server run test:tool-registry`; `pnpm --filter api-server run test:route-guards`; `pnpm --filter api-server typecheck`; `pnpm --filter localai-control-center typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build`; `pnpm run verify:jarvis`; scoped `git diff --check -- <Phase 07B files>` | COMPLETE: Docker MCP targeted test passed; tool-registry test passed; route-guard test passed with 43 assertions; full tests, typecheck, build-kit verifier, Jarvis verifier, UI build, and scoped whitespace check passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 07C. |
| Phase 07C OpenClaw and NemoClaw gateway safety wrappers | `artifacts/api-server/src/lib/claw-gateway.ts`; `artifacts/api-server/src/lib/tool-registry.ts`; `artifacts/api-server/src/routes/plugins.ts`; `artifacts/api-server/tests/claw-gateway.test.ts`; `artifacts/api-server/tests/tool-registry.test.ts`; `artifacts/api-server/tests/docker-mcp-gateway.test.ts`; `artifacts/api-server/tests/route-guard-coverage.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Integrations.tsx`; Jarvis docs | `pnpm --filter api-server run test:claw-gateway`; `pnpm --filter api-server run test:tool-registry`; `pnpm --filter api-server run test:docker-mcp`; `pnpm --filter api-server run test:route-guards`; `pnpm --filter api-server typecheck`; `pnpm --filter localai-control-center typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build`; `pnpm run verify:jarvis`; scoped `git diff --check -- <Phase 07C files>` | COMPLETE: OpenClaw/NemoClaw targeted test passed; tool-registry test passed; Docker MCP regression test passed; route-guard test passed with 45 assertions; full tests, typecheck, build-kit verifier, Jarvis verifier, UI build, and scoped whitespace check passed | Deferred blockers only: B-003, B-004, B-005, B-006, B-008, B-009, B-010, B-011 | Cleared for Phase 08A. |
| Phase 08A Professional RAG engine and document ingestion interfaces | `artifacts/api-server/src/lib/rag.ts`; `artifacts/api-server/src/routes/rag.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/tests/rag.test.ts`; `artifacts/api-server/tests/foundation.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Workspace.tsx`; Jarvis docs | `pnpm --filter api-server run test:rag`; `pnpm --filter api-server typecheck`; `pnpm --filter localai-control-center typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build`; `pnpm run verify:jarvis` | COMPLETE: targeted RAG test passed with 45 assertions; full tests, typecheck, build-kit verifier, Jarvis verifier, and UI build passed. `foundation.test.ts` was tightened to avoid leasing an older persistent queued job from the reused local SQLite DB. | Superseded by blocker repair pass; B-009 remains deferred only | Cleared for Phase 08B. |
| Deferred blocker repair pass | `artifacts/api-server/src/routes/worldgui.ts`; `artifacts/api-server/tests/permission-routes.test.ts`; `artifacts/api-server/src/lib/runtime-diagnostics.ts`; `artifacts/api-server/src/routes/workspace.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/src/routes/repair.ts`; `artifacts/api-server/src/routes/studios.ts`; `artifacts/api-server/src/routes/web.ts`; `artifacts/api-server/src/lib/studio-pipeline.ts`; `artifacts/api-server/package.json`; `pnpm-lock.yaml`; `README.md`; Jarvis docs | `pnpm --filter api-server run test:permission-routes`; `pnpm --filter api-server run test:runtime-diagnostics`; `pnpm --filter api-server typecheck`; `pnpm audit --prod`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm run verify:jarvis`; `pnpm --filter localai-control-center build`; host `pwsh`/`powershell.exe`/bundled Node/`nvidia-smi` checks | COMPLETE: permission routes passed with 174 assertions; runtime diagnostics passed with 8 assertions; API typecheck, full typecheck, full tests, build-kit verifier, Jarvis verifier, UI build, audit, host PowerShell, browser Node fallback, and NVML telemetry checks passed. | B-009 remains deferred only | Cleared for Phase 08B; future durable executor work must not fake execution. |
| Phase 08B Evidence Vault and Paperless/Manuals/Receipts Workflow | `artifacts/api-server/src/lib/evidence-vault.ts`; `artifacts/api-server/src/routes/evidence.ts`; `artifacts/api-server/src/routes/index.ts`; `artifacts/api-server/src/lib/thought-log.ts`; `artifacts/api-server/tests/evidence-vault.test.ts`; `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/EvidenceVault.tsx`; `artifacts/localai-control-center/src/App.tsx`; Jarvis docs | `pnpm --filter api-server run test:evidence`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: evidence-vault targeted test passed with 87 assertions; full tests, typecheck, build-kit verifier, and UI build passed. Prior phase tests unaffected. UI build emitted `EvidenceVault-BBoedN05.js`. | B-009 remains deferred only | Cleared for Phase 09A. |
| Phase 09A Browser Automation with Playwright MCP Safety | `artifacts/api-server/src/lib/playwright-browser.ts` (NEW); `artifacts/api-server/src/lib/tool-registry.ts`; `artifacts/api-server/src/routes/plugins.ts`; `artifacts/api-server/tests/playwright-browser.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Integrations.tsx`; Jarvis docs | `pnpm --filter api-server run test:playwright-browser`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: playwright-browser targeted test passed with 32 assertions; full tests (all prior suites unaffected), typecheck, and UI build passed on 2026-04-29. UI build emitted `Integrations-CEmVIEKA.js`. | B-009 remains deferred only | Cleared for Phase 09B. Hard limits (credential/anti-bot/cookie) must never be weakened. |
| Phase 09B Desktop/App Automation Drivers With WorldGUI Fallback | `artifacts/api-server/src/lib/desktop-automation.ts` (NEW); `artifacts/api-server/src/lib/tool-registry.ts`; `artifacts/api-server/src/routes/plugins.ts`; `artifacts/api-server/tests/desktop-automation.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Integrations.tsx`; Jarvis docs | `pnpm --filter api-server run test:desktop-automation`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: desktop-automation targeted test passed with 39 assertions; full tests (all prior suites unaffected — 32+87+32+... assertions), typecheck, and UI build passed on 2026-04-29. UI build emitted `Integrations-xMpY-DL4.js`. | B-009 remains deferred only | Cleared for Phase 10. Hard limits (credential entry/keylogger/sensitive-screenshot) must never be weakened. |
| Phase 10 Chat-Driven Program Modification And Coding Agent Runtime | `artifacts/api-server/src/lib/coding-agent.ts` (NEW); `artifacts/api-server/src/lib/tool-registry.ts`; `artifacts/api-server/src/routes/intelligence.ts`; `artifacts/api-server/src/routes/plugins.ts`; `artifacts/api-server/tests/coding-agent.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Integrations.tsx`; Jarvis docs | `pnpm --filter api-server run test:coding-agent`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: coding-agent targeted test passed with 45 assertions; full tests (all prior suites unaffected), typecheck, and UI build passed on 2026-04-30. UI build emitted `Integrations-Db7fgBJ_.js`. | B-009 remains deferred only | Cleared for Phase 11. Hard limits (selfModification/directMainApply/destructiveCommands) must never be weakened. Approval gate on execute-refactor must remain. |
| Phase 11 Voice, Screen Context, Meeting Intelligence, and Local Interaction Modes | `artifacts/api-server/src/lib/voice-meeting.ts` (NEW); `artifacts/api-server/src/routes/voice.ts` (NEW); `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/src/routes/index.ts`; `artifacts/api-server/src/lib/thought-log.ts`; `artifacts/api-server/tests/voice-meeting.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Voice.tsx` (NEW); `artifacts/localai-control-center/src/App.tsx`; Jarvis docs | `pnpm --filter api-server run test:voice-meeting`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: voice-meeting targeted test passed with 50 assertions; full tests (all prior suites unaffected), typecheck, build-kit verifier, and UI build passed on 2026-04-30. UI build emitted `Voice-Bm1r7m1W.js`. | B-009 remains deferred only | Cleared for Phase 12A. Hard limits (alwaysOnCapture/cloudSTT/cloudTTS/screenpipe/followUpAutoSend) must never be weakened. captureMode defaults to "disabled". |
| Phase 12A Business Module Foundation And CRM/Support Adapters | `artifacts/api-server/src/lib/business-modules.ts` (NEW); `artifacts/api-server/src/routes/business.ts` (NEW); `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/src/routes/index.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/tests/business-modules.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Business.tsx` (NEW); `artifacts/localai-control-center/src/pages/Integrations.tsx`; `artifacts/localai-control-center/src/App.tsx`; Jarvis docs | `pnpm --filter api-server run test:business`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: business-modules targeted test passed with 11 assertions; full tests, typecheck, build-kit verifier, and UI build passed on 2026-04-30. UI build emitted `Business-BW-CJx9R.js`. Default tests require no Docker, Python, network, cloud APIs, or external services. | B-009 remains deferred only | Cleared for Phase 12B only. Hard limits (no stealth bots, spam blasting, anti-bot evasion, or unapproved external sends) must never be weakened. Chatwoot/Twenty/Cal/Postiz/email/SMS adapters remain disabled/not_configured until a later approved service-specific executor phase. |
| Phase 12B IT Support Copilot And Safe Script Generator | `artifacts/api-server/src/lib/it-support.ts` (NEW); `artifacts/api-server/src/routes/it-support.ts` (NEW); `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/src/lib/command-sanitizer.ts`; `artifacts/api-server/src/routes/index.ts`; `artifacts/api-server/tests/it-support.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/ITSupport.tsx` (NEW); `artifacts/localai-control-center/src/App.tsx`; Jarvis docs | `pnpm --filter api-server run test:it-support`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: it-support targeted test passed with 11 assertions; full tests, typecheck, build-kit verifier, and UI build passed on 2026-04-30. UI build emitted `ITSupport-DWQvoBfh.js`. Default tests require no Docker, Python, network, cloud APIs, optional IT providers, or external services. | B-009 remains deferred only | Cleared for Phase 13A only. Generated scripts must remain review/dry-run by default; approved real execution remains not_configured until a later service-specific executor phase. |
| Phase 13A Maker Studio Foundation: Project, CAD, Material, And Safety Model | `artifacts/api-server/src/lib/maker-studio.ts` (NEW); `artifacts/api-server/src/routes/studios.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/tests/maker-studio.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Studios.tsx`; Jarvis docs | `pnpm --filter api-server run test:maker-studio`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: maker-studio targeted test passed with 9 assertions; full tests, typecheck, build-kit verifier, and UI build passed on 2026-04-30. UI build emitted `Studios-CIwkX3p6.js`. Default tests require no Docker, Python, network, cloud APIs, FreeCAD, KiCad, slicers, printers, CNC, laser, electronics bench, or external services. | B-009 remains deferred only | Cleared for Phase 13B only. Maker Studio must remain a foundation/control layer; real CAD/slicer/printer/CNC/laser/electronics execution stays disabled/not_configured/manual-only until later explicit approved executor phases. |
| Phase 13B FreeCAD, CAD-as-Code, And KiCad Adapters | `artifacts/api-server/src/lib/maker-studio.ts`; `artifacts/api-server/src/routes/studios.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/tests/maker-cad-adapters.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Studios.tsx`; Jarvis docs | `pnpm --filter api-server run test:maker-cad`; `pnpm --filter api-server run test:maker-studio`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: maker-cad targeted test passed with 7 assertions; maker-studio regression test passed with 9 assertions; full tests, typecheck, build-kit verifier, and UI build passed on 2026-04-30. Default tests require no Docker, Python, network, cloud APIs, FreeCAD, KiCad, CadQuery, build123d, OpenSCAD, PCB tools, hardware, or external services. | B-009 remains deferred only | Cleared for Phase 13C only. FreeCAD/CAD-as-code/KiCad remain proposal-only/not_configured until a later explicit approved executor; cloud text-to-CAD remains disabled/not_configured by default. |
| Phase 13C 3D Printer, Slicer, Spoolman, And Obico Workflow | `artifacts/api-server/src/lib/maker-studio.ts`; `artifacts/api-server/src/routes/studios.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/tests/maker-print-workflow.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Studios.tsx`; Jarvis docs | `pnpm --filter api-server run test:maker-print`; `pnpm --filter api-server run test:maker-studio`; `pnpm --filter api-server run test:maker-cad`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: maker-print targeted test passed with 8 assertions; maker-studio regression test passed with 9 assertions; maker-cad regression test passed with 7 assertions; full tests, typecheck, build-kit verifier, and UI build passed on 2026-04-30. Default tests require no Docker, Python, network, cloud APIs, slicers, printers, Spoolman, Obico, FDM Monster, hardware, or external services. | B-009 remains deferred only | Cleared for Phase 13D only. Slicer/printer/Spoolman/Obico integrations remain proposal-only/not_configured/disabled until a later explicit approved executor; print start/heater/motor actions stay approval-gated or blocked. |
| Phase 13D CNC, Laser, CAM, And Electronics Bench Safety Console | `artifacts/api-server/src/lib/maker-studio.ts`; `artifacts/api-server/src/routes/studios.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/tests/maker-machine-safety.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Studios.tsx`; Jarvis docs | `pnpm --filter api-server run test:maker-machine`; `pnpm --filter api-server run test:maker-print`; `pnpm --filter api-server run test:maker-cad`; `pnpm --filter api-server run test:maker-studio`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: maker-machine targeted test passed with 8 assertions; maker-print regression test passed with 8 assertions; maker-studio regression test passed with 9 assertions; maker-cad regression test passed with 7 assertions; full tests, typecheck, build-kit verifier, and UI build passed on 2026-04-30. Default tests require no Docker, Python, network, cloud APIs, FreeCAD Path/CAM, CNCjs, LinuxCNC, FluidNC, bCNC, LightBurn, KiCad electronics tools, serial/USB devices, machine APIs, hardware, or external services. | B-009 remains deferred only | Cleared for Phase 14A only. CNC/laser/CAM/electronics integrations remain proposal-only/not_configured/disabled/manual-only until a later explicit approved executor; G-code send, machine motion, spindle, laser, firmware, relay/power, serial/USB, and dangerous bench actions stay blocked/manual-only/approval-gated. |
| Phase 14A Edge Node Architecture And Home/Shop Autopilot Foundation | `artifacts/api-server/src/lib/edge-node.ts` (NEW); `artifacts/api-server/src/routes/remote.ts`; `artifacts/api-server/tests/edge-node.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Remote.tsx`; Jarvis docs | `pnpm --filter api-server run test:edge-node`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: edge-node targeted test passed with 27 assertions; full tests (all prior suites unaffected), typecheck, build-kit verifier, and UI build passed on 2026-04-30. UI build emitted `Remote-DmF7pR02.js`. Default tests require no real edge nodes, home/shop APIs, remote node services, network, Docker, or external services. | B-009 remains deferred only | Cleared for Phase 14B. Gaming PC `alwaysOn` hard limit must never be removed. Camera frame capture and shop relay/power hard limits must never be weakened. No services must be installed to remote nodes without a later explicit approved executor phase. |
| Phase 14B Home Assistant, Robot Vacuum, Cameras, MQTT, And Shop Devices | `artifacts/api-server/src/lib/home-autopilot.ts` (NEW); `artifacts/api-server/src/routes/remote.ts`; `artifacts/api-server/tests/home-autopilot.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Remote.tsx`; Jarvis docs | `pnpm --filter api-server run test:home-autopilot`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: home-autopilot targeted test passed with 41 assertions; full tests (all prior suites unaffected), typecheck, build-kit verifier, and UI build passed on 2026-04-30. UI build emitted `Remote-C8wran71.js`. Default tests require no Home Assistant, MQTT broker, robot vacuum, Frigate NVR, shop devices, Docker, network, or external services. | B-009 remains deferred only | Cleared for Phase 15A. `camera_frame_capture`/snapshot/recording hard block and compressor/main-power `manual_only` and all `APPROVAL_REQUIRED_HOME_ACTIONS` gates must never be weakened. `executed: false` literal type must remain on all eval result types. |
| Phase 15A HomeLab Architect Source Of Truth, NetBox/Nautobot, Inventory, And Diagrams | `artifacts/api-server/src/lib/homelab-architect.ts` (NEW); `artifacts/api-server/src/routes/homelab.ts` (NEW); `artifacts/api-server/src/routes/index.ts`; `artifacts/api-server/tests/homelab-architect.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/HomeLab.tsx` (NEW); `artifacts/localai-control-center/src/App.tsx`; Jarvis docs | `pnpm --filter api-server run test:homelab`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: homelab-architect targeted test passed with 42 assertions; full tests (all prior suites unaffected — 47+45+45+174+33+15+8+15+21+25+32+15+66+45+32+87+32+39+45+50+11+11+9+7+8+8+27+41+42 assertions), typecheck, and UI build passed on 2026-04-30. UI build emitted `HomeLab-Dq11wUmu.js`. Default tests require no NetBox, Nautobot, Proxmox, OPNsense, UniFi, Ansible, OpenTofu, Batfish, Docker, network, cloud APIs, or external services. | B-009 remains deferred only | Cleared for Phase 15B. `applied: false` TypeScript literal type on `HomelabBlueprint` must never be changed; all 8 optional provider defaults must remain `not_configured`; no config push, firewall/VLAN/DNS/DHCP/routing/device change must be added without a later explicit approved executor phase. Privacy log filter (no raw IPs/managementIpRef/serial/credentials in thought log) must be preserved. |
| Phase 15B HomeLab Config Generation, Validation, And Apply Pipeline | `artifacts/api-server/src/lib/homelab-architect.ts`; `artifacts/api-server/src/routes/homelab.ts`; `artifacts/api-server/tests/homelab-config-pipeline.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/HomeLab.tsx`; Jarvis docs | `pnpm --dir artifacts/api-server run test:homelab-config`; `pnpm --dir artifacts/api-server run test:homelab`; `pnpm --dir artifacts/api-server run typecheck`; `pnpm --dir artifacts/localai-control-center run typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: homelab-config targeted test passed with 16 assertions; homelab-architect regression test passed with 42 assertions; API/UI targeted typechecks passed; full build-kit/typecheck/test/UI build checks passed on 2026-04-30. Default tests require no NetBox, Nautobot, Proxmox, OPNsense, UniFi, Ansible, OpenTofu/Terraform, Docker Compose, Batfish, Docker, Python, network, cloud APIs, or external services. | B-009 remains deferred only | Cleared for Phase 16. HomeLab apply must remain blocked/not_configured until configured provider, validation proof, approval, policy allowance, and backup/rollback proof all exist; denied apply must never execute. |
| Phase 16 Home SOC And Security Monitoring Copilot | `artifacts/api-server/src/lib/homelab-architect.ts`; `artifacts/api-server/src/routes/homelab.ts`; `artifacts/api-server/tests/homelab-soc.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/HomeLab.tsx`; Jarvis docs | `pnpm --dir artifacts/api-server run test:homelab-soc`; `pnpm --dir artifacts/api-server run test:homelab-config`; `pnpm --dir artifacts/api-server run test:homelab`; `pnpm --dir artifacts/api-server run typecheck`; `pnpm --dir artifacts/localai-control-center run typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: homelab-soc targeted test passed with 14 assertions; homelab-config and homelab-architect regressions passed; API/UI targeted typechecks passed; full build-kit/typecheck/test/UI build checks passed on 2026-04-30. Default tests require no Wazuh, Zeek, Suricata, OPNsense IDS/IPS, Pi-hole, AdGuard Home, LibreNMS, Zabbix, Netdata, Uptime Kuma, osquery, firewall/router/security APIs, packet capture, Docker, Python, network, cloud APIs, or external services. | B-009 remains deferred only | Cleared for Phase 17A. Home SOC must remain read-first/proposal/approval-gated; packet capture stays blocked; approved remediations stay not_configured until a later service-specific executor exists. |
| Phase 17A Digital Twin Core For Home, Shop, Network, Vehicles, Tools, And Projects | `artifacts/api-server/src/lib/digital-twin.ts` (NEW); `artifacts/api-server/src/routes/context.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/tests/digital-twin.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/App.tsx`; `artifacts/localai-control-center/src/pages/DigitalTwin.tsx` (NEW); Jarvis docs | `pnpm --dir artifacts/api-server run test:digital-twin`; source-regression suites for Home SOC/HomeLab/Maker/Edge/Home Autopilot/Evidence; `pnpm --dir artifacts/api-server run typecheck`; `pnpm --dir artifacts/localai-control-center run typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: digital-twin targeted test passed with 11 assertions; source-regression suites passed when run sequentially where the shared SQLite file requires isolation; API/UI targeted typechecks passed. Default tests require no Docker, Python, network, cloud APIs, discovery scans, HomeLab/SOC/Maker/edge/home/vehicle providers, device APIs, or external services. | B-009 remains deferred only | Cleared for Phase 17B. Digital Twin must remain a local source-of-truth/relationship layer; unknowns must not be guessed; physical actions must continue delegating to existing safety policies with `executed: false`. |
| Phase 17B Inventory, Parts, Tools, Spool, Asset, And Project-To-Reality Pipeline | `artifacts/api-server/src/lib/inventory-pipeline.ts` (NEW); `artifacts/api-server/src/lib/digital-twin.ts`; `artifacts/api-server/src/routes/context.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/tests/inventory-pipeline.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/App.tsx`; `artifacts/localai-control-center/src/pages/Inventory.tsx` (NEW); Jarvis docs | `pnpm --dir artifacts/api-server run test:inventory`; `pnpm --dir artifacts/api-server run typecheck`; `pnpm --dir artifacts/localai-control-center run typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: inventory targeted test passed with 11 assertions; API/UI targeted typechecks passed; build-kit verifier, workspace typecheck, full tests, and UI build passed on 2026-05-01. Default tests require no InvenTree, Snipe-IT, HomeBox, Spoolman, PartKeepr, vendor APIs, scanners, NFC writers, label printers, Docker, Python, network, cloud APIs, or external services. | B-009 remains deferred only | Cleared for Phase 18. Inventory/project-to-reality must remain local-first; unknown availability must not be guessed; purchase/reorder/vendor/label/NFC/delete actions remain approval-gated proposal-only with `executed:false` until a later explicit approved executor phase. |
| Phase 18 Automotive Mechanic And Vehicle Diagnostics Assistant | `artifacts/api-server/src/lib/automotive-diagnostics.ts` (NEW); `artifacts/api-server/src/lib/digital-twin.ts`; `artifacts/api-server/src/routes/context.ts`; `artifacts/api-server/src/routes/integrations.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/tests/automotive-diagnostics.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/App.tsx`; `artifacts/localai-control-center/src/pages/Automotive.tsx` (NEW); Jarvis docs | `pnpm --dir artifacts/api-server run test:automotive`; `pnpm --dir artifacts/api-server run typecheck`; `pnpm --dir artifacts/localai-control-center run typecheck`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: automotive targeted test passed with 12 assertions; API/UI targeted typechecks passed; build-kit verifier, workspace typecheck, full tests, and UI build passed on 2026-05-01. Default tests require no python-OBD, ELM327, ELM327-emulator, SavvyCAN, OVMS, ACES ECU APIs/log providers, CAN interfaces, vehicle scanners, serial/Bluetooth devices, Docker, Python, network, cloud APIs, or external services. | B-009 remains deferred only | Cleared for Phase 19. Automotive diagnostics must remain local-first and test-before-parts; likely causes are not confirmed faults without evidence; clear-code/CAN/actuator/bidirectional actions remain approval-gated; ECU write/tune/firmware actions remain manual_only until a later explicit approved executor phase. |
| Phase 19 Robotics Lab Future Layer | `artifacts/api-server/src/lib/robotics-lab.ts` (NEW); `artifacts/api-server/src/routes/studios.ts`; `artifacts/api-server/tests/robotics-lab.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Studios.tsx`; Jarvis docs | `pnpm --filter api-server run test:robotics`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: robotics-lab targeted test passed with 16 assertions; `pnpm -r typecheck` passed (6 type errors fixed: `AuditEventInput` fields, `verifyApprovedRequest` 3-arg signature, `ApprovalVerification.allowed/approval?.status/message`, `ApprovalRequestInput` correct fields); full tests (all prior suites unaffected), and UI build passed on 2026-05-01; `Studios-CHd7BYb4.js` emitted. Default tests require no ROS 2, MoveIt 2, Nav2, Gazebo, Ignition Gazebo, depth camera, rosbridge, Foxglove, Docker, Python, network, cloud APIs, serial/USB, or physical hardware. | B-009 remains deferred only | Cleared for Phase 20. `execute_motion` and `navigate` are permanently blocked — no approval can unblock them. `gripper_open/close`, `arm_move`, `firmware_flash`, `relay_toggle`, `serial_write` are `manual_only`. `physicalHardwarePresent: false`, `simulationOnly: true`, `hardwareExecutionBlocked: true`, `executed: false` are TypeScript literal types and must never be changed. |
| Phase 20 UI/UX Integration And Control Center Polish | `artifacts/localai-control-center/src/components/StatusBadges.tsx` (NEW); `artifacts/localai-control-center/src/App.tsx` (NAV_GROUPS); `artifacts/localai-control-center/src/pages/Dashboard.tsx` (3 status cards + strip); `artifacts/localai-control-center/tests/ui-integration.test.tsx` (NEW); `artifacts/localai-control-center/package.json`; Jarvis docs | `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test` (including new `test:ui-integration`); `pnpm --filter localai-control-center build` | COMPLETE: `ui-integration.test.tsx` passed with 24 assertions; `pnpm -r typecheck` passed with no errors; full test suite (all prior API + UI suites unaffected) passed on 2026-05-01; `Dashboard-vdsQgTNt.js` and `index-D3sLYcX0.js` emitted. No DOM or browser required for tests (renderToStaticMarkup + QueryClient.setQueryData). No routes/pages/safety systems removed or redesigned. | B-009 remains deferred only | Cleared for Phase 21. StatusPill fallback, UnavailableCard honest-state contract, and Dashboard query key alignment must be preserved. No fake-ready states may be added to any phase. |
| Phase 21 Packaging, Install, Backup, Restore, And Disaster Recovery | `artifacts/api-server/src/lib/packaging-recovery.ts` (NEW); `artifacts/api-server/src/routes/system.ts`; `artifacts/api-server/src/db/schema.ts`; `artifacts/api-server/src/db/migrate.ts`; `artifacts/api-server/tests/packaging-recovery.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Operations.tsx`; root recovery scripts; `package.json`; `README.md`; Jarvis docs | `pnpm --dir artifacts/api-server run test:packaging-recovery`; `pnpm --dir artifacts/api-server run typecheck`; `pnpm --dir artifacts/localai-control-center run typecheck`; `node scripts/health-check.mjs`; `node scripts/restore-config.mjs --dry-run`; `node scripts/gaming-mode.mjs --dry-run`; `node scripts/emergency-stop.mjs --dry-run`; `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: packaging-recovery targeted test passed with 49 assertions; targeted API/UI typechecks and helper script dry-runs passed; build-kit verification passed; recursive typecheck passed; full API/UI test suite passed; localai-control-center production build passed. Default tests require no Docker, Python, network, cloud APIs, external backup/install providers, service manager, firewall, PATH changes, startup tasks, or destructive restore. | B-009 remains deferred only | Cleared for Phase 22. Recovery must remain metadata-only/local-first by default; restore stays dry-run/approval-gated and real destructive restore remains not_configured until a later explicit approved executor. |
| Phase 22 Local AI Transition: Make LOCALAI Capable Of Building Itself | `artifacts/api-server/src/lib/local-builder.ts` (NEW); `artifacts/api-server/src/routes/intelligence.ts` (routes appended); `artifacts/api-server/tests/local-builder.test.ts` (NEW); `artifacts/api-server/package.json`; `artifacts/localai-control-center/src/api.ts`; `artifacts/localai-control-center/src/pages/Studios.tsx`; `docs/context-packs/core-architecture.md` (NEW); `docs/context-packs/safety-and-permissions.md` (NEW); `docs/context-packs/current-build-state.md` (NEW); `docs/context-packs/next-phase-template.md` (NEW); Jarvis docs | `pnpm --filter api-server run test:local-builder`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: local-builder targeted test passed with 21 assertions; `pnpm -r typecheck` passed (6 type errors fixed: `AuditEventInput outcome→result+eventType`, `thoughtLog.publish()` call, `ALL_ROLES` export); full test suite (all prior suites unaffected) passed on 2026-05-01; `Studios-NYszjgNe.js` emitted. Default tests require no Docker, Python, network, cloud APIs, Ollama models, or external services. | B-009 remains deferred only | Cleared for Phase 23. `cloudEscalationEnabled=false`, `selfModificationAllowed=false`, `requireApprovalForEdits=true` TypeScript literal types must never be changed. Shell metacharacter and self-modification hard blocks must never be weakened. Evals must remain usedNetwork=false. Context packs must stay in docs/context-packs/ and never be deleted. |
| Phase 23 Final Coverage Audit And Gap Closer | `docs/JARVIS_UI_STYLE_GUARD.md` (Phase 22 UI note); `docs/JARVIS_REQUIREMENTS_TRACEABILITY.md` (final-audit row updated); `docs/JARVIS_EXPERT_MODES.md` (Phase 22 Maintainer note); `docs/JARVIS_CONTEXT_INDEX.md` (Phase 22 + Phase 19 Studios entries); `docs/JARVIS_IMPLEMENTATION_LEDGER.md` (Phase 23 entry + current phase); `docs/JARVIS_PHASE_MAP.md` (Phase 23 Complete); `docs/JARVIS_BLOCKERS.md` (Phase 23 note + B-012); `docs/JARVIS_TEST_MATRIX.md` (Phase 23 row); `docs/JARVIS_LOCAL_AI_HANDOFF.md` (current state); `docs/JARVIS_FINAL_PRESTART_REVIEW.md` (Phase 23 note) | `node scripts/jarvis/verify-build-kit.mjs`; `pnpm -r typecheck`; `pnpm test`; `pnpm --filter localai-control-center build` | COMPLETE: verifier passed; `pnpm -r typecheck` passed; full test suite passed (all prior suites unaffected — no new code); `pnpm --filter localai-control-center build` passed on 2026-05-02. No new code files, routes, tests, or schema changes. All doc changes additive; no safety gates weakened. | B-009 and B-012 deferred only | All 30+ requirements verified implemented or in active blockers. Future phases must address B-009 executor follow-through and B-012 Project Foreman cross-system workflow. |
